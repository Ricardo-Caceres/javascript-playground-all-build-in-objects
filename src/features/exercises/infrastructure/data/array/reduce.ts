import type { Exercise } from '@/shared/types/exercises'

export const reduceExercises: Exercise[] = [
  {
    slug: 'array-reduce-sum',
    title: 'Array.prototype.reduce() — sum all elements',
    description: `## Array.prototype.reduce()\n\n\`reduce()\` executes a reducer function on each element, accumulating a single result.\n\n\`\`\`ts\n[1, 2, 3].reduce((acc, n) => acc + n, 0) // 6\n\`\`\`\n\nThe second argument (\`0\`) is the **initial value** of the accumulator.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Array',
    method: 'Array.prototype.reduce',
    initialCode: `function sum(nums: number[]): number {\n  // Use reduce to sum all elements, starting from 0\n}`,
    solution: `function sum(nums: number[]): number {\n  return nums.reduce((acc, n) => acc + n, 0)\n}`,
    tests: [
      { description: 'sums [1, 2, 3] to 6', assertion: `expect(sum([1, 2, 3])).toBe(6)` },
      { description: 'returns 0 for empty array', assertion: `expect(sum([])).toBe(0)` },
      { description: 'returns the value for a single-element array', assertion: `expect(sum([42])).toBe(42)` },
      { description: 'handles negative numbers', assertion: `expect(sum([-1, -2, -3])).toBe(-6)` },
      { description: 'sums mixed positive and negative numbers', assertion: `expect(sum([10, -3, 2])).toBe(9)` },
    ],
    hints: [
      'The reducer signature is `(accumulator, currentValue) => newAccumulator`.',
      'Always provide `0` as the initial value to handle empty arrays safely.',
    ],
    tags: ['array', 'reduce', 'sum', 'accumulator'],
  },
  {
    slug: 'array-reduce-product',
    title: 'Array.prototype.reduce() — product of all elements',
    description: `## reduce() with initial value 1\n\nUse \`reduce()\` to multiply all elements together. The initial value for a product is **1** (the multiplicative identity).\n\n\`\`\`ts\n[1, 2, 3, 4].reduce((acc, n) => acc * n, 1) // 24\n\`\`\``,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Array',
    method: 'Array.prototype.reduce',
    initialCode: `function product(nums: number[]): number {\n  // Use reduce to multiply all elements; initial value is 1\n}`,
    solution: `function product(nums: number[]): number {\n  return nums.reduce((acc, n) => acc * n, 1)\n}`,
    tests: [
      { description: 'multiplies [1, 2, 3, 4] to 24', assertion: `expect(product([1, 2, 3, 4])).toBe(24)` },
      { description: 'returns 1 for empty array', assertion: `expect(product([])).toBe(1)` },
      { description: 'returns the element itself for a single-element array', assertion: `expect(product([7])).toBe(7)` },
      { description: 'returns 0 when any element is 0', assertion: `expect(product([1, 2, 0, 4])).toBe(0)` },
      { description: 'handles negative numbers', assertion: `expect(product([-2, 3])).toBe(-6)` },
    ],
    hints: [
      'Use `1` as the initial value — using `0` would make the product always zero.',
      'The reducer is `(acc, n) => acc * n`.',
    ],
    tags: ['array', 'reduce', 'product', 'multiplication'],
  },
  {
    slug: 'array-reduce-flatten',
    title: 'Array.prototype.reduce() — flatten nested arrays',
    description: `## Flattening with reduce + concat\n\nYou can flatten a 2D array using \`reduce()\` with \`concat()\`.\n\n\`\`\`ts\n[[1, 2], [3, 4]].reduce((acc, cur) => acc.concat(cur), []) // [1, 2, 3, 4]\n\`\`\``,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Array',
    method: 'Array.prototype.reduce',
    initialCode: `function flatten(arr: number[][]): number[] {\n  // Use reduce + concat to flatten a 2D array\n}`,
    solution: `function flatten(arr: number[][]): number[] {\n  return arr.reduce((acc, cur) => acc.concat(cur), [] as number[])\n}`,
    tests: [
      { description: 'flattens a 2D array', assertion: `expect(flatten([[1, 2], [3, 4]])).toEqual([1, 2, 3, 4])` },
      { description: 'returns empty array for empty input', assertion: `expect(flatten([])).toEqual([])` },
      { description: 'handles sub-arrays of different lengths', assertion: `expect(flatten([[1], [2, 3], [4, 5, 6]])).toEqual([1, 2, 3, 4, 5, 6])` },
      { description: 'handles empty sub-arrays', assertion: `expect(flatten([[], [1, 2], []])).toEqual([1, 2])` },
      { description: 'preserves element order', assertion: `expect(flatten([[3, 1], [4, 1], [5]])).toEqual([3, 1, 4, 1, 5])` },
    ],
    hints: [
      'Start with `[]` as the initial accumulator.',
      'Use `acc.concat(cur)` to merge each sub-array into the accumulator.',
      'TypeScript needs `[] as number[]` or `<number[]>[]` for the initial value.',
    ],
    tags: ['array', 'reduce', 'flatten', 'concat'],
  },
  {
    slug: 'array-reduce-count-occurrences',
    title: 'Array.prototype.reduce() — count word occurrences',
    description: `## Building an object with reduce\n\n\`reduce()\` can build any value — including objects. Use it to count how many times each string appears.\n\n\`\`\`ts\ncountOccurrences(['a', 'b', 'a']) // { a: 2, b: 1 }\n\`\`\``,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Array',
    method: 'Array.prototype.reduce',
    initialCode: `function countOccurrences(arr: string[]): Record<string, number> {\n  // Use reduce to build an occurrence count object\n}`,
    solution: `function countOccurrences(arr: string[]): Record<string, number> {\n  return arr.reduce<Record<string, number>>((acc, word) => {\n    acc[word] = (acc[word] ?? 0) + 1\n    return acc\n  }, {})\n}`,
    tests: [
      { description: 'counts occurrences of each string', assertion: `expect(countOccurrences(['a', 'b', 'a'])).toEqual({a: 2, b: 1})` },
      { description: 'returns empty object for empty array', assertion: `expect(countOccurrences([])).toEqual({})` },
      { description: 'counts a single occurrence correctly', assertion: `expect(countOccurrences(['x'])).toEqual({x: 1})` },
      { description: 'handles three different words', assertion: `expect(countOccurrences(['cat', 'dog', 'cat', 'bird', 'dog', 'dog'])).toEqual({cat: 2, dog: 3, bird: 1})` },
      { description: 'counts all-same elements', assertion: `expect(countOccurrences(['z', 'z', 'z'])).toEqual({z: 3})` },
    ],
    hints: [
      'Use `{}` as the initial accumulator typed as `Record<string, number>`.',
      'Use `acc[word] = (acc[word] ?? 0) + 1` to safely increment without checking existence first.',
      'Remember to `return acc` at the end of the reducer.',
    ],
    tags: ['array', 'reduce', 'object', 'count', 'frequency'],
  },
  {
    slug: 'array-reduce-max',
    title: 'Array.prototype.reduce() — find the maximum value',
    description: `## Finding max with reduce\n\nYou can find the maximum value in an array using \`reduce()\` — without \`Math.max\`.\n\n\`\`\`ts\n[3, 1, 4, 1, 5, 9].reduce((max, cur) => cur > max ? cur : max, -Infinity) // 9\n\`\`\``,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Array',
    method: 'Array.prototype.reduce',
    initialCode: `function findMax(nums: number[]): number {\n  // Use reduce to find the max — without using Math.max\n}`,
    solution: `function findMax(nums: number[]): number {\n  return nums.reduce((max, cur) => cur > max ? cur : max, -Infinity)\n}`,
    tests: [
      { description: 'finds the max in a typical array', assertion: `expect(findMax([3, 1, 4, 1, 5, 9, 2, 6])).toBe(9)` },
      { description: 'returns the only element for a single-element array', assertion: `expect(findMax([42])).toBe(42)` },
      { description: 'handles all negative numbers', assertion: `expect(findMax([-5, -1, -3])).toBe(-1)` },
      { description: 'handles an array with duplicate values', assertion: `expect(findMax([7, 7, 7])).toBe(7)` },
      { description: 'handles the first element being the max', assertion: `expect(findMax([100, 1, 2, 3])).toBe(100)` },
    ],
    hints: [
      'Use `-Infinity` as the initial value so any real number beats it.',
      'The reducer is `(max, cur) => cur > max ? cur : max`.',
      'Do not use `Math.max` — solve it purely with `reduce`.',
    ],
    tags: ['array', 'reduce', 'max', 'comparison'],
  },
]
