import type { Exercise } from '@/shared/types/exercises'

export const reverseExercises: Exercise[] = [
  {
    slug: 'array-reverse-basic',
    title: 'Array.prototype.reverse() — non-mutating reverse',
    description: `## Array.prototype.reverse()\n\n\`reverse()\` **mutates** the original array in place. To avoid mutation, copy the array first with \`slice()\` or the spread operator.\n\n\`\`\`ts\nconst reversed = arr.slice().reverse() // original unchanged\n\`\`\``,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Array',
    method: 'Array.prototype.reverse',
    initialCode: `function reverseArray(arr: number[]): number[] {\n  // Return a reversed copy without mutating the original\n}`,
    solution: `function reverseArray(arr: number[]): number[] {\n  return arr.slice().reverse()\n}`,
    tests: [
      { description: 'reverses [1, 2, 3] to [3, 2, 1]', assertion: `expect(reverseArray([1, 2, 3])).toEqual([3, 2, 1])` },
      { description: 'does not mutate the original array', assertion: `const a = [1, 2, 3]; reverseArray(a); expect(a).toEqual([1, 2, 3])` },
      { description: 'handles a single-element array', assertion: `expect(reverseArray([5])).toEqual([5])` },
      { description: 'handles an empty array', assertion: `expect(reverseArray([])).toEqual([])` },
      { description: 'reverses a two-element array', assertion: `expect(reverseArray([10, 20])).toEqual([20, 10])` },
    ],
    hints: [
      'Call `.slice()` first to create a shallow copy, then call `.reverse()` on the copy.',
      'Alternatively, use the spread operator: `[...arr].reverse()`.',
    ],
    tags: ['array', 'reverse', 'immutable', 'copy'],
  },
  {
    slug: 'array-reverse-string',
    title: 'Array.prototype.reverse() — reverse characters of a string',
    description: `## Reversing a string with reverse()\n\nStrings don't have a \`reverse()\` method, but you can split into an array of characters, reverse, then join back.\n\n\`\`\`ts\n'hello'.split('').reverse().join('') // 'olleh'\n\`\`\``,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Array',
    method: 'Array.prototype.reverse',
    initialCode: `function reverseChars(str: string): string {\n  // Split str into chars, reverse the array, and join back\n}`,
    solution: `function reverseChars(str: string): string {\n  return str.split('').reverse().join('')\n}`,
    tests: [
      { description: 'reverses "hello" to "olleh"', assertion: `expect(reverseChars('hello')).toBe('olleh')` },
      { description: 'returns a single character unchanged', assertion: `expect(reverseChars('a')).toBe('a')` },
      { description: 'returns empty string for empty input', assertion: `expect(reverseChars('')).toBe('')` },
      { description: 'reverses "abcde" to "edcba"', assertion: `expect(reverseChars('abcde')).toBe('edcba')` },
      { description: 'a palindrome string reverses to itself', assertion: `expect(reverseChars('racecar')).toBe('racecar')` },
    ],
    hints: [
      'Split with `split(\'\')` (empty string delimiter) to get individual characters.',
      'Chain: `.split(\'\').reverse().join(\'\')` in one expression.',
    ],
    tags: ['array', 'reverse', 'string', 'split', 'join'],
  },
  {
    slug: 'array-reverse-mutates',
    title: 'Array.prototype.reverse() — in-place mutation',
    description: `## reverse() mutates the original array\n\n\`reverse()\` reverses the array **in place** and returns a reference to the **same** array.\n\n\`\`\`ts\nconst arr = [1, 2, 3]\nconst result = arr.reverse()\nresult === arr // true — same reference\n\`\`\``,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Array',
    method: 'Array.prototype.reverse',
    initialCode: `function reverseInPlace(arr: number[]): number[] {\n  // Reverse arr in place and return the same array reference\n}`,
    solution: `function reverseInPlace(arr: number[]): number[] {\n  return arr.reverse()\n}`,
    tests: [
      { description: 'mutates and reverses the original array', assertion: `const a = [1, 2, 3]; reverseInPlace(a); expect(a).toEqual([3, 2, 1])` },
      { description: 'returns the same array reference', assertion: `const a = [1, 2, 3]; expect(reverseInPlace(a)).toBe(a)` },
      { description: 'returns [3, 2, 1] for input [1, 2, 3]', assertion: `expect(reverseInPlace([1, 2, 3])).toEqual([3, 2, 1])` },
      { description: 'handles a two-element array', assertion: `expect(reverseInPlace([4, 5])).toEqual([5, 4])` },
      { description: 'returns empty array unchanged', assertion: `expect(reverseInPlace([])).toEqual([])` },
    ],
    hints: [
      'Just call `arr.reverse()` and return its result.',
      '`reverse()` returns the same mutated array, not a copy.',
    ],
    tags: ['array', 'reverse', 'mutation', 'in-place', 'reference'],
  },
  {
    slug: 'array-reverse-words',
    title: 'Array.prototype.reverse() — reverse word order in a sentence',
    description: `## Reversing words in a sentence\n\nSplit the sentence into words, reverse the word array, then join them back together.\n\n\`\`\`ts\n'hello world'.split(' ').reverse().join(' ') // 'world hello'\n\`\`\``,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Array',
    method: 'Array.prototype.reverse',
    initialCode: `function reverseWords(sentence: string): string {\n  // Split on spaces, reverse the word array, join back with spaces\n}`,
    solution: `function reverseWords(sentence: string): string {\n  return sentence.split(' ').reverse().join(' ')\n}`,
    tests: [
      { description: 'reverses a two-word sentence', assertion: `expect(reverseWords('hello world')).toBe('world hello')` },
      { description: 'reverses a three-word sentence', assertion: `expect(reverseWords('one two three')).toBe('three two one')` },
      { description: 'returns a single word unchanged', assertion: `expect(reverseWords('only')).toBe('only')` },
      { description: 'preserves individual word content', assertion: `expect(reverseWords('foo bar baz')).toContain('foo')` },
      { description: 'result starts with the last word of the input', assertion: `expect(reverseWords('a b c').startsWith('c')).toBeTruthy()` },
    ],
    hints: [
      'Split on `\' \'` (single space) to get an array of words.',
      'Chain: `.split(\' \').reverse().join(\' \')`.',
    ],
    tags: ['array', 'reverse', 'string', 'words', 'split-join'],
  },
  {
    slug: 'array-reverse-palindrome',
    title: 'Array.prototype.reverse() — check if array is a palindrome',
    description: `## Palindrome check\n\nAn array is a palindrome if it reads the same forwards and backwards: \`[1, 2, 1]\` is a palindrome, \`[1, 2, 3]\` is not.\n\nCompare each element with its mirror index to check efficiently.\n\n\`\`\`ts\narr.every((val, i) => val === arr[arr.length - 1 - i])\n\`\`\``,
    category: 'instance-method',
    difficulty: 'advanced',
    builtIn: 'Array',
    method: 'Array.prototype.reverse',
    initialCode: `function isPalindrome(arr: number[]): boolean {\n  // Return true if arr reads the same forwards and backwards\n}`,
    solution: `function isPalindrome(arr: number[]): boolean {\n  return arr.every((val, i) => val === arr[arr.length - 1 - i])\n}`,
    tests: [
      { description: 'returns true for [1, 2, 1]', assertion: `expect(isPalindrome([1, 2, 1])).toBe(true)` },
      { description: 'returns false for [1, 2, 3]', assertion: `expect(isPalindrome([1, 2, 3])).toBe(false)` },
      { description: 'returns true for an empty array', assertion: `expect(isPalindrome([])).toBe(true)` },
      { description: 'returns true for a single-element array', assertion: `expect(isPalindrome([7])).toBe(true)` },
      { description: 'returns true for [1, 2, 3, 2, 1]', assertion: `expect(isPalindrome([1, 2, 3, 2, 1])).toBe(true)` },
    ],
    hints: [
      'Use `arr.every((val, i) => val === arr[arr.length - 1 - i])` to compare each element with its mirror.',
      'The mirror index of `i` in an array of length `n` is `n - 1 - i`.',
    ],
    tags: ['array', 'reverse', 'palindrome', 'every', 'symmetric'],
  },
]
