import type { Exercise } from '@/shared/types/exercises'

export const toLocaleStringExercises: Exercise[] = [
  {
    slug: 'array-to-locale-string-basic',
    title: 'Localize a Number Array',
    description: `## Array.prototype.toLocaleString\n\n\`toLocaleString()\` converts each element to a locale-sensitive string and joins them with a locale-appropriate separator.\n\nImplement \`localizeArray\` that returns \`arr.toLocaleString()\`.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Array',
    method: 'Array.prototype.toLocaleString',
    initialCode: `function localizeArray(arr: number[]): string {\n  // Use arr.toLocaleString()\n}`,
    solution: `function localizeArray(arr: number[]): string {\n  return arr.toLocaleString()\n}`,
    tests: [
      { description: 'returns a string', assertion: "expect(typeof localizeArray([1,2,3])).toBe('string')" },
      { description: 'empty array returns empty string', assertion: "expect(localizeArray([])).toBe('')" },
      { description: 'single element returns a string', assertion: "expect(typeof localizeArray([42])).toBe('string')" },
      { description: 'result is truthy for non-empty array', assertion: 'expect(localizeArray([1,2,3])).toBeTruthy()' },
      { description: 'result has length > 0 for non-empty array', assertion: 'expect(localizeArray([1]).length > 0).toBe(true)' },
    ],
    hints: [
      '`toLocaleString()` on an array calls `toLocaleString()` on each element and joins with a locale separator.',
      'The exact output depends on the runtime locale, so avoid testing the precise formatted value.',
    ],
    tags: ['array', 'toLocaleString', 'locale', 'string'],
  },
  {
    slug: 'array-to-locale-string-currency',
    title: 'Format Amounts as Currency',
    description: `## Array.prototype.toLocaleString — Currency Formatting\n\nCombine \`Array.prototype.map\` with \`Number.prototype.toLocaleString\` to format each amount as a USD currency string, then join them.\n\nImplement \`formatCurrencies(amounts)\` that maps each number to \`n.toLocaleString('en-US', {style:'currency', currency:'USD'})\` and joins with \`', '\`.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Array',
    method: 'Array.prototype.toLocaleString',
    initialCode: `function formatCurrencies(amounts: number[]): string {\n  // Map each amount to a USD currency string and join with ', '\n}`,
    solution: `function formatCurrencies(amounts: number[]): string {\n  return amounts.map(n => n.toLocaleString('en-US', { style: 'currency', currency: 'USD' })).join(', ')\n}`,
    tests: [
      { description: 'result contains dollar sign', assertion: "expect(formatCurrencies([1])).toContain('$')" },
      { description: 'result contains dollar sign for multiple amounts', assertion: "expect(formatCurrencies([1, 2]).includes('$')).toBe(true)" },
      { description: 'returns a string', assertion: "expect(typeof formatCurrencies([100])).toBe('string')" },
      { description: 'empty array returns empty string', assertion: "expect(formatCurrencies([])).toBe('')" },
      { description: 'result is truthy for non-empty array', assertion: 'expect(formatCurrencies([10])).toBeTruthy()' },
    ],
    hints: [
      'Use `.map()` to convert each number, then `.join()` to combine results.',
      'Pass `{ style: "currency", currency: "USD" }` as the second argument to `toLocaleString`.',
    ],
    tags: ['array', 'toLocaleString', 'currency', 'map', 'locale'],
  },
  {
    slug: 'array-to-locale-string-dates',
    title: 'Format an Array of Dates',
    description: `## Array.prototype.toLocaleString — Date Arrays\n\nWhen called on an array of \`Date\` objects, \`toLocaleString()\` converts each date to a locale-sensitive date string.\n\nImplement \`formatDates(dates)\` that returns \`dates.toLocaleString()\`.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Array',
    method: 'Array.prototype.toLocaleString',
    initialCode: `function formatDates(dates: Date[]): string {\n  // Use dates.toLocaleString()\n}`,
    solution: `function formatDates(dates: Date[]): string {\n  return dates.toLocaleString()\n}`,
    tests: [
      { description: 'returns a string', assertion: "expect(typeof formatDates([new Date('2023-01-01')])).toBe('string')" },
      { description: 'result is truthy for non-empty array', assertion: "expect(formatDates([new Date('2023-06-15')])).toBeTruthy()" },
      { description: 'empty array returns empty string', assertion: "expect(formatDates([])).toBe('')" },
      { description: 'two dates returns a string', assertion: "expect(typeof formatDates([new Date('2023-01-01'), new Date('2023-12-31')])).toBe('string')" },
      { description: 'result length > 0 for non-empty array', assertion: "expect(formatDates([new Date('2023-01-01')]).length > 0).toBe(true)" },
    ],
    hints: [
      '`toLocaleString()` on an array calls `toLocaleString()` on each element.',
      'Each `Date` object has its own `toLocaleString()` that formats according to the runtime locale.',
    ],
    tags: ['array', 'toLocaleString', 'date', 'locale'],
  },
  {
    slug: 'array-to-locale-string-mixed',
    title: 'Mixed Array toLocaleString',
    description: `## Array.prototype.toLocaleString — Mixed Types\n\n\`toLocaleString()\` works on arrays containing mixed types. Each element's own \`toLocaleString()\` is called.\n\nImplement \`mixedToLocale(arr)\` that returns \`arr.toLocaleString()\` for an array of numbers and strings.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Array',
    method: 'Array.prototype.toLocaleString',
    initialCode: `function mixedToLocale(arr: (number | string)[]): string {\n  // Use arr.toLocaleString()\n}`,
    solution: `function mixedToLocale(arr: (number | string)[]): string {\n  return arr.toLocaleString()\n}`,
    tests: [
      { description: 'returns a string', assertion: "expect(typeof mixedToLocale([1, 'hello'])).toBe('string')" },
      { description: 'returns a string for numbers only', assertion: "expect(typeof mixedToLocale([1, 2, 3])).toBe('string')" },
      { description: 'returns a string for strings only', assertion: "expect(typeof mixedToLocale(['a', 'b'])).toBe('string')" },
      { description: 'empty array returns empty string', assertion: "expect(mixedToLocale([])).toBe('')" },
      { description: 'result is truthy for non-empty array', assertion: "expect(mixedToLocale([1, 'hello'])).toBeTruthy()" },
    ],
    hints: [
      'Strings use their own `toLocaleString()`, which just returns the string itself.',
      'Numbers use `Number.prototype.toLocaleString()` for locale-sensitive formatting.',
    ],
    tags: ['array', 'toLocaleString', 'mixed', 'locale'],
  },
  {
    slug: 'array-to-locale-string-empty',
    title: 'Empty Array toLocaleString',
    description: `## Array.prototype.toLocaleString — Empty Array\n\nCalling \`toLocaleString()\` on an empty array returns an empty string \`''\`. This is consistent with \`toString()\` and \`join()\` behavior on empty arrays.\n\nImplement \`emptyLocaleString()\` that returns \`([] as number[]).toLocaleString()\`.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Array',
    method: 'Array.prototype.toLocaleString',
    initialCode: `function emptyLocaleString(): string {\n  // Return ([] as number[]).toLocaleString()\n}`,
    solution: `function emptyLocaleString(): string {\n  return ([] as number[]).toLocaleString()\n}`,
    tests: [
      { description: "returns empty string ''", assertion: "expect(emptyLocaleString()).toBe('')" },
      { description: 'returns a string type', assertion: "expect(typeof emptyLocaleString()).toBe('string')" },
      { description: 'result is falsy', assertion: 'expect(emptyLocaleString()).toBeFalsy()' },
      { description: 'has length 0', assertion: 'expect(emptyLocaleString()).toHaveLength(0)' },
      { description: 'strict equality with empty string', assertion: "expect(emptyLocaleString() === '').toBe(true)" },
    ],
    hints: [
      'An empty array has no elements to convert, so `toLocaleString()` returns `""`.',
      'This mirrors the behavior of `[].toString()` and `[].join(",")`.',
    ],
    tags: ['array', 'toLocaleString', 'empty', 'edge-case'],
  },
]
