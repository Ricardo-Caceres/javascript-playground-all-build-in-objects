import type { Exercise } from '@/shared/types/exercises'

export const toStringExercises: Exercise[] = [
  {
    slug: 'array-to-string-basic',
    title: 'Convert Number Array to String',
    description: `## Array.prototype.toString\n\n\`toString\` joins all array elements into a single comma-separated string — equivalent to calling \`join(',')\`.\n\nImplement \`arrayToString\` which returns the string representation of a number array.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Array',
    method: 'Array.prototype.toString',
    initialCode: `function arrayToString(arr: number[]): string {\n  // return the string representation\n}`,
    solution: `function arrayToString(arr: number[]): string {\n  return arr.toString()\n}`,
    tests: [
      { description: 'converts numbers to comma-separated string', assertion: `expect(arrayToString([1, 2, 3])).toBe('1,2,3')` },
      { description: 'handles single element', assertion: `expect(arrayToString([42])).toBe('42')` },
      { description: 'handles empty array', assertion: `expect(arrayToString([])).toBe('')` },
      { description: 'return type is string', assertion: `expect(typeof arrayToString([1, 2, 3])).toBe('string')` },
      { description: 'handles negative numbers', assertion: `expect(arrayToString([-1, -2, -3])).toBe('-1,-2,-3')` },
    ],
    hints: [
      'Simply call arr.toString() — no separator argument needed',
      'The default separator is a comma',
    ],
    tags: ['array', 'toString', 'string', 'conversion'],
  },
  {
    slug: 'array-to-string-strings',
    title: 'Convert String Array to String',
    description: `## Array.prototype.toString — String Arrays\n\n\`toString\` works on arrays of any type, not just numbers. It joins all elements with commas.\n\nImplement \`stringArrayToString\` which returns the comma-separated string representation of a string array.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Array',
    method: 'Array.prototype.toString',
    initialCode: `function stringArrayToString(arr: string[]): string {\n  // return the string representation\n}`,
    solution: `function stringArrayToString(arr: string[]): string {\n  return arr.toString()\n}`,
    tests: [
      { description: 'joins strings with commas', assertion: `expect(stringArrayToString(['a', 'b', 'c'])).toBe('a,b,c')` },
      { description: 'handles single string', assertion: `expect(stringArrayToString(['hello'])).toBe('hello')` },
      { description: 'handles empty array', assertion: `expect(stringArrayToString([])).toBe('')` },
      { description: 'handles multi-character strings', assertion: `expect(stringArrayToString(['foo', 'bar'])).toBe('foo,bar')` },
      { description: 'return type is string', assertion: `expect(typeof stringArrayToString(['x', 'y'])).toBe('string')` },
    ],
    hints: [
      'Call arr.toString() directly — it works the same way for strings',
      'The result is always a plain string, not an array',
    ],
    tags: ['array', 'toString', 'strings', 'conversion'],
  },
  {
    slug: 'array-to-string-same-as-join',
    title: 'toString Equals join with Comma',
    description: `## Array.prototype.toString — Equivalence to join\n\n\`arr.toString()\` produces the exact same result as \`arr.join(',')\`. This is a useful equivalence to know when choosing between the two.\n\nImplement \`verifyToString\` which returns \`true\` if \`arr.toString()\` strictly equals \`arr.join(',')\`.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Array',
    method: 'Array.prototype.toString',
    initialCode: `function verifyToString(arr: number[]): boolean {\n  // return true if toString() === join(',')\n}`,
    solution: `function verifyToString(arr: number[]): boolean {\n  return arr.toString() === arr.join(',')\n}`,
    tests: [
      { description: 'returns true for number array', assertion: `expect(verifyToString([1, 2, 3])).toBe(true)` },
      { description: 'returns true for single element', assertion: `expect(verifyToString([42])).toBe(true)` },
      { description: 'returns true for empty array', assertion: `expect(verifyToString([])).toBe(true)` },
      { description: 'returns true for negative numbers', assertion: `expect(verifyToString([-1, 0, 1])).toBe(true)` },
      { description: 'return type is boolean', assertion: `expect(typeof verifyToString([1, 2, 3])).toBe('boolean')` },
    ],
    hints: [
      'Use === to compare the two string results',
      'Both toString() and join(\',\') use a comma separator by default',
    ],
    tags: ['array', 'toString', 'join', 'equivalence'],
  },
  {
    slug: 'array-to-string-nested',
    title: 'toString with Nested Arrays',
    description: `## Array.prototype.toString — Nested Arrays\n\nWhen an array contains nested arrays, \`toString\` recursively flattens them into the output string. Each nested array is also comma-joined.\n\nImplement \`nestedToString\` which returns the string representation of an array that may contain nested arrays.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Array',
    method: 'Array.prototype.toString',
    initialCode: `function nestedToString(arr: (number | number[])[]): string {\n  // return the string representation of a nested array\n}`,
    solution: `function nestedToString(arr: (number | number[])[]): string {\n  return arr.toString()\n}`,
    tests: [
      { description: 'flattens nested array in output', assertion: `expect(nestedToString([1, [2, 3], 4])).toBe('1,2,3,4')` },
      { description: 'handles deeply nested values', assertion: `expect(nestedToString([[1, 2], [3, 4]])).toBe('1,2,3,4')` },
      { description: 'handles mix of numbers and arrays', assertion: `expect(nestedToString([1, [2], 3])).toBe('1,2,3')` },
      { description: 'handles single nested array', assertion: `expect(nestedToString([[1, 2, 3]])).toBe('1,2,3')` },
      { description: 'return type is string', assertion: `expect(typeof nestedToString([1, [2, 3]])).toBe('string')` },
    ],
    hints: [
      'toString recursively calls toString on each element, including nested arrays',
      'No extra flattening step is needed — just call arr.toString()',
    ],
    tags: ['array', 'toString', 'nested', 'recursive'],
  },
  {
    slug: 'array-to-string-empty',
    title: 'toString of Empty Array',
    description: `## Array.prototype.toString — Empty Array\n\nCalling \`toString\` on an empty array returns an empty string \`''\`. This is consistent with \`[].join(',')\` also returning \`''\`.\n\nImplement \`emptyToString\` which returns the result of calling \`toString()\` on an empty number array.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Array',
    method: 'Array.prototype.toString',
    initialCode: `function emptyToString(): string {\n  // return toString of an empty array\n}`,
    solution: `function emptyToString(): string {\n  return ([] as number[]).toString()\n}`,
    tests: [
      { description: 'returns empty string', assertion: `expect(emptyToString()).toBe('')` },
      { description: 'return type is string', assertion: `expect(typeof emptyToString()).toBe('string')` },
      { description: 'has length 0', assertion: `expect(emptyToString()).toHaveLength(0)` },
      { description: 'is falsy', assertion: `expect(emptyToString()).toBeFalsy()` },
      { description: 'equals empty join', assertion: `expect(emptyToString() === [].join(',')).toBe(true)` },
    ],
    hints: [
      'Create an empty array literal and call toString() on it',
      'Use a type assertion ([] as number[]) to satisfy TypeScript',
    ],
    tags: ['array', 'toString', 'empty', 'edge-case'],
  },
]
