import type { Exercise } from '@/shared/types/exercises'

export const valuesExercises: Exercise[] = [
  {
    slug: 'array-values-basic',
    title: 'Get Array Values as Array',
    description: `## Array.prototype.values\n\n\`values()\` returns an **Array Iterator** that yields each value in the array. You can convert it to a plain array using \`Array.from\`.\n\nImplement \`getValues\` which converts \`arr.values()\` to a regular array using \`Array.from\`.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Array',
    method: 'Array.prototype.values',
    initialCode: `function getValues(arr: number[]): number[] {\n  // convert arr.values() to an array\n}`,
    solution: `function getValues(arr: number[]): number[] {\n  return Array.from(arr.values())\n}`,
    tests: [
      { description: 'returns all values', assertion: `expect(getValues([1, 2, 3])).toEqual([1, 2, 3])` },
      { description: 'handles empty array', assertion: `expect(getValues([])).toEqual([])` },
      { description: 'handles single element', assertion: `expect(getValues([42])).toEqual([42])` },
      { description: 'result has correct length', assertion: `expect(getValues([10, 20, 30])).toHaveLength(3)` },
      { description: 'returns a plain array', assertion: `expect(Array.isArray(getValues([1, 2, 3]))).toBe(true)` },
    ],
    hints: [
      'Use Array.from(arr.values()) to materialise the iterator into an array',
      'The result contains the same values in the same order as the original',
    ],
    tags: ['array', 'values', 'iterator', 'Array.from'],
  },
  {
    slug: 'array-values-iterate',
    title: 'Iterate with values()',
    description: `## Array.prototype.values — for...of\n\nThe iterator returned by \`values()\` can be consumed with a \`for...of\` loop, giving you each element one at a time.\n\nImplement \`collectValues\` which uses a \`for...of\` loop on \`arr.values()\` to collect all elements into a new array.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Array',
    method: 'Array.prototype.values',
    initialCode: `function collectValues(arr: string[]): string[] {\n  // use for...of on arr.values() to collect elements\n}`,
    solution: `function collectValues(arr: string[]): string[] {\n  const result: string[] = []\n  for (const val of arr.values()) {\n    result.push(val)\n  }\n  return result\n}`,
    tests: [
      { description: 'collects all string values', assertion: `expect(collectValues(['a', 'b', 'c'])).toEqual(['a', 'b', 'c'])` },
      { description: 'handles empty array', assertion: `expect(collectValues([])).toEqual([])` },
      { description: 'handles single element', assertion: `expect(collectValues(['hello'])).toEqual(['hello'])` },
      { description: 'preserves order', assertion: `expect(collectValues(['z', 'y', 'x'])[0]).toBe('z')` },
      { description: 'result has correct length', assertion: `expect(collectValues(['a', 'b', 'c', 'd'])).toHaveLength(4)` },
    ],
    hints: [
      'Use for (const val of arr.values()) and push each val into a result array',
      'arr.values() yields values in index order, just like iterating arr directly',
    ],
    tags: ['array', 'values', 'iterator', 'for-of'],
  },
  {
    slug: 'array-values-vs-keys',
    title: 'Compare values() and keys()',
    description: `## Array.prototype.values — vs keys()\n\n\`arr.keys()\` yields the indices, while \`arr.values()\` yields the elements. Both are iterators and work with \`Array.from\`.\n\nImplement \`valuesAndKeys\` which returns an object with both \`keys\` and \`values\` arrays derived from the respective iterators.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Array',
    method: 'Array.prototype.values',
    initialCode: `function valuesAndKeys(arr: number[]): { keys: number[]; values: number[] } {\n  // return { keys: Array.from(arr.keys()), values: Array.from(arr.values()) }\n}`,
    solution: `function valuesAndKeys(arr: number[]): { keys: number[]; values: number[] } {\n  return { keys: Array.from(arr.keys()), values: Array.from(arr.values()) }\n}`,
    tests: [
      { description: 'returns correct keys and values', assertion: `expect(valuesAndKeys([10, 20, 30])).toEqual({ keys: [0, 1, 2], values: [10, 20, 30] })` },
      { description: 'keys are always 0-based indices', assertion: `expect(valuesAndKeys([99, 88])).toEqual({ keys: [0, 1], values: [99, 88] })` },
      { description: 'both arrays have the same length', assertion: `expect(valuesAndKeys([1, 2, 3]).keys).toHaveLength(3)` },
      { description: 'handles single element', assertion: `expect(valuesAndKeys([42])).toEqual({ keys: [0], values: [42] })` },
      { description: 'handles empty array', assertion: `expect(valuesAndKeys([])).toEqual({ keys: [], values: [] })` },
    ],
    hints: [
      'arr.keys() yields 0, 1, 2, ... — the indices',
      'arr.values() yields the actual element values at each index',
    ],
    tags: ['array', 'values', 'keys', 'iterator', 'entries'],
  },
  {
    slug: 'array-values-spread',
    title: 'Spread an Array Values Iterator',
    description: `## Array.prototype.values — Spread\n\nBecause \`values()\` returns an iterable iterator, you can spread it with \`[...arr.values()]\` to get a new array — a concise alternative to \`Array.from\`.\n\nImplement \`spreadValues\` which uses the spread operator on \`arr.values()\` to produce a new array.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Array',
    method: 'Array.prototype.values',
    initialCode: `function spreadValues(arr: number[]): number[] {\n  // spread arr.values() into a new array\n}`,
    solution: `function spreadValues(arr: number[]): number[] {\n  return [...arr.values()]\n}`,
    tests: [
      { description: 'produces same elements as original', assertion: `expect(spreadValues([1, 2, 3])).toEqual([1, 2, 3])` },
      { description: 'handles empty array', assertion: `expect(spreadValues([])).toEqual([])` },
      { description: 'handles single element', assertion: `expect(spreadValues([7])).toEqual([7])` },
      { description: 'result has correct length', assertion: `expect(spreadValues([10, 20, 30, 40])).toHaveLength(4)` },
      { description: 'result is a plain array', assertion: `expect(Array.isArray(spreadValues([1, 2, 3]))).toBe(true)` },
    ],
    hints: [
      'Use [...arr.values()] — the spread operator consumes any iterable',
      'This is equivalent to Array.from(arr.values())',
    ],
    tags: ['array', 'values', 'iterator', 'spread'],
  },
  {
    slug: 'array-values-same-as-iter',
    title: 'values() Is the Default Iterator',
    description: `## Array.prototype.values — Default Iterator\n\nThe default array iterator (\`arr[Symbol.iterator]\`) is identical to \`arr.values\`. Both yield elements in index order, so spreading them produces the same result.\n\nImplement \`valuesIteratorIsDefault\` which returns \`true\` if \`[...arr[Symbol.iterator]()].join()\` equals \`[...arr.values()].join()\`.`,
    category: 'instance-method',
    difficulty: 'advanced',
    builtIn: 'Array',
    method: 'Array.prototype.values',
    initialCode: `function valuesIteratorIsDefault(arr: number[]): boolean {\n  // verify Symbol.iterator and values() produce the same output\n}`,
    solution: `function valuesIteratorIsDefault(arr: number[]): boolean {\n  return [...arr[Symbol.iterator]()].join() === [...arr.values()].join()\n}`,
    tests: [
      { description: 'returns true for a number array', assertion: `expect(valuesIteratorIsDefault([1, 2, 3])).toBe(true)` },
      { description: 'returns true for empty array', assertion: `expect(valuesIteratorIsDefault([])).toBe(true)` },
      { description: 'returns true for single element', assertion: `expect(valuesIteratorIsDefault([42])).toBe(true)` },
      { description: 'returns true for longer array', assertion: `expect(valuesIteratorIsDefault([10, 20, 30])).toBe(true)` },
      { description: 'return type is boolean', assertion: `expect(typeof valuesIteratorIsDefault([1, 2, 3])).toBe('boolean')` },
    ],
    hints: [
      'arr[Symbol.iterator] and arr.values reference the same underlying function',
      'Use .join() to compare the two iterator outputs as strings',
    ],
    tags: ['array', 'values', 'Symbol.iterator', 'iterator', 'advanced'],
  },
]
