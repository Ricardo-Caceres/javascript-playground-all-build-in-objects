import type { Exercise } from '@/shared/types/exercises'

export const asyncAwaitExercises: Exercise[] = [
  {
    slug: 'async-await-1',
    title: 'async function returns Promise',
    description: `## Async Functions\n\n\`async function f() { return 42 }\` declares an async function. Any return value is wrapped in a resolved Promise.\n\n**Challenge:** Verify that an async function returns a Promise.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'AsyncPatterns',
    initialCode: `async function f() { return 42 }\nf() instanceof Promise\n`,
    solution: `async function f() { return 42 } f() instanceof Promise`,
    tests: [
      { description: 'async fn returns Promise', assertion: 'async function f(){return 42} expect(f() instanceof Promise).toBe(true)' },
      { description: 'async with no return', assertion: 'async function f(){} expect(f() instanceof Promise).toBe(true)' },
      { description: 'async with expression', assertion: 'async function f(){return 1+1} expect(f() instanceof Promise).toBe(true)' },
      { description: 'multiple calls are different instances', assertion: 'async function f(){return 1} expect(f() !== f()).toBe(true)' },
      { description: 'typeof result is object', assertion: 'async function f(){return 1} expect(typeof f()).toBe("object")' },
    ],
    hints: ['All async functions return Promises.', 'The returned value is automatically wrapped.'],
    tags: ['async', 'function', 'promise'],
  },
  {
    slug: 'async-await-2',
    title: 'async arrow function returns Promise',
    description: `## Async Arrow Functions\n\n\`const f = async () => 99\` is an async arrow function. It also returns a Promise.\n\n**Challenge:** Verify that an async arrow function returns a Promise.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'AsyncPatterns',
    initialCode: `const f = async () => 99\nf() instanceof Promise\n`,
    solution: `const f = async () => 99; f() instanceof Promise`,
    tests: [
      { description: 'async arrow function returns Promise', assertion: 'const f=async()=>99; expect(f() instanceof Promise).toBe(true)' },
      { description: 'async arrow with block body', assertion: 'const f=async()=>{return 1}; expect(f() instanceof Promise).toBe(true)' },
      { description: 'async arrow with no return', assertion: 'const f=async()=>{}; expect(f() instanceof Promise).toBe(true)' },
      { description: 'multiple calls are different instances', assertion: 'const f=async()=>1; expect(f() !== f()).toBe(true)' },
      { description: 'typeof result is object', assertion: 'const f=async()=>1; expect(typeof f()).toBe("object")' },
    ],
    hints: ['Async arrow functions work the same as async regular functions.', 'They return Promises.'],
    tags: ['async', 'arrow', 'promise'],
  },
  {
    slug: 'async-await-3',
    title: 'Promise.then callbacks are asynchronous (microtask)',
    description: `## Microtask Queue\n\n\`.then()\` callbacks are executed asynchronously as microtasks. They run after synchronous code but before macrotasks.\n\n**Challenge:** Verify that a .then callback does not run synchronously.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'AsyncPatterns',
    initialCode: `let x = 0\nPromise.resolve().then(() => { x = 1 })\n// x is still 0 here\n`,
    solution: `let x = 0; Promise.resolve().then(() => { x = 1 }); x`,
    tests: [
      { description: 'Promise.resolve().then callback is async (microtask)', assertion: 'let x=0; Promise.resolve().then(()=>{x=1}); expect(x).toBe(0)' },
      { description: 'callback does not run before microtasks flush', assertion: 'let called=false; Promise.resolve().then(()=>{called=true}); expect(called).toBe(false)' },
      { description: 'synchronous code runs first', assertion: 'let order=[]; order.push(1); Promise.resolve().then(()=>order.push(2)); order.push(3); expect(order[0]).toBe(1)' },
      { description: 'order contains only sync pushes before microtasks', assertion: 'let order=[]; order.push(1); Promise.resolve().then(()=>order.push(2)); order.push(3); expect(order).toEqual([1,3])' },
      { description: 'multiple .then callbacks queue', assertion: 'let count=0; Promise.resolve().then(()=>{count++}).then(()=>{count++}); expect(count).toBe(0)' },
    ],
    hints: ['.then callbacks are microtasks, not synchronous.', 'They queue after the current execution context.', 'This is key to understanding the Promise execution model.'],
    tags: ['promise', 'then', 'microtask', 'async'],
  },
  {
    slug: 'async-await-4',
    title: 'Executor runs sync, then handler is async',
    description: `## Executor vs Then\n\nThe executor function runs synchronously, but .then() handlers run asynchronously as microtasks.\n\n**Challenge:** Verify the execution order: executor sync, then handler async.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'AsyncPatterns',
    initialCode: `let order = []\nnew Promise(resolve => {\n  order.push(1)\n  resolve()\n}).then(() => order.push(3))\norder.push(2)\n// order is [1, 2] at this point\n`,
    solution: `let order=[]; new Promise(r=>{order.push(1);r()}).then(()=>order.push(3)); order.push(2); order`,
    tests: [
      { description: 'executor runs sync then handler is async', assertion: 'let order=[]; new Promise(r=>{order.push(1);r()}).then(()=>order.push(3)); order.push(2); expect(order).toEqual([1,2])' },
      { description: 'first sync code then microtask', assertion: 'let order=[]; new Promise(r=>r()).then(()=>order.push("b")); order.push("a"); expect(order[0]).toBe("a")' },
      { description: 'chain shows ordering', assertion: 'let order=[]; new Promise(r=>{order.push(1);r(2)}).then(v=>order.push(v)); order.push(3); expect(order).toEqual([1,3,2])' },
      { description: 'multiple thens queue in order', assertion: 'let order=[]; new Promise(r=>{order.push(1);r()}).then(()=>order.push(2)).then(()=>order.push(3)); order.push(4); expect(order).toEqual([1,4])' },
      { description: 'catch also queues as microtask', assertion: 'let order=[]; new Promise(r=>r()).catch(()=>{order.push(2)}); order.push(1); expect(order[0]).toBe(1)' },
    ],
    hints: ['The executor function runs synchronously.', '.then() and .catch() handlers run asynchronously as microtasks.', 'Synchronous code after the Promise runs before microtask handlers.'],
    tags: ['promise', 'executor', 'then', 'microtask', 'execution-order'],
  },
  {
    slug: 'async-await-5',
    title: 'async function with no return resolves undefined',
    description: `## Async Function Resolution\n\nAn async function with no explicit return statement resolves to \`undefined\`.\n\n**Challenge:** Verify that an async function without return still returns a Promise.`,
    category: 'instance-method',
    difficulty: 'advanced',
    builtIn: 'AsyncPatterns',
    initialCode: `async function f() {}\nf() instanceof Promise\n`,
    solution: `async function f() {} f() instanceof Promise`,
    tests: [
      { description: 'async fn with no return resolves undefined', assertion: 'async function f(){} const p=f(); expect(p instanceof Promise).toBe(true)' },
      { description: 'empty async resolves to Promise', assertion: 'async function f(){} expect(f() instanceof Promise).toBe(true)' },
      { description: 'async with only side effects returns Promise', assertion: 'async function f(){let x=1;} expect(f() instanceof Promise).toBe(true)' },
      { description: 'multiple async calls return different instances', assertion: 'async function f(){} const p1=f(); const p2=f(); expect(p1 !== p2).toBe(true)' },
      { description: 'typeof result is object', assertion: 'async function f(){} expect(typeof f()).toBe("object")' },
    ],
    hints: ['All async functions return Promises.', 'No return means the Promise resolves to undefined.', 'Each call returns a new Promise instance.'],
    tags: ['async', 'function', 'promise', 'undefined'],
  },
]
