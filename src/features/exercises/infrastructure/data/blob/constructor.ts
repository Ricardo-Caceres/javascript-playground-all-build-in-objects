import type { Exercise } from '@/shared/types/exercises'

export const blobConstructorExercises: Exercise[] = [
  {
    slug: 'blob-constructor-1',
    title: "Blob — size of 'hello'",
    description: `## Blob Constructor\n\n\`new Blob(blobParts, options)\` creates a Blob from an array of strings, ArrayBuffers, or other Blobs.\n\n\`new Blob(['hello']).size\` equals **5** because \`'hello'\` has 5 UTF-8 bytes.\n\n**Challenge:** Verify that \`new Blob(['hello']).size === 5\`.`,
    category: 'constructor',
    difficulty: 'beginner',
    builtIn: 'Blob',
    initialCode: `// Create a Blob and check its size\nconst b = new Blob(['hello'])\n`,
    solution: `new Blob(['hello']).size`,
    tests: [
      { description: "size of 'hello' is 5", assertion: "expect(new Blob(['hello']).size).toBe(5)" },
      { description: 'size is a number', assertion: "expect(typeof new Blob(['hello']).size).toBe('number')" },
      { description: "size of 'world' is 5", assertion: "expect(new Blob(['world']).size).toBe(5)" },
      { description: "size of 'hi' is 2", assertion: "expect(new Blob(['hi']).size).toBe(2)" },
      { description: 'instanceof Blob', assertion: "expect(new Blob(['hello']) instanceof Blob).toBe(true)" },
    ],
    hints: ['Blob.size reflects the number of UTF-8 bytes.'],
    tags: ['Blob', 'constructor', 'size'],
  },
  {
    slug: 'blob-constructor-2',
    title: 'Blob — type option',
    description: `## Blob MIME Type\n\nPass a \`{ type }\` option to set the MIME type of the Blob.\n\n**Challenge:** Verify that \`new Blob(['hello'], { type: 'text/plain' }).type === 'text/plain'\`.`,
    category: 'constructor',
    difficulty: 'beginner',
    builtIn: 'Blob',
    initialCode: `// Create a Blob with a MIME type\nconst b = new Blob(['hello'], { type: 'text/plain' })\n`,
    solution: `new Blob(['hello'], { type: 'text/plain' }).type`,
    tests: [
      { description: "type is 'text/plain'", assertion: "expect(new Blob(['hello'], { type: 'text/plain' }).type).toBe('text/plain')" },
      { description: "type is 'application/json'", assertion: "expect(new Blob(['{}'], { type: 'application/json' }).type).toBe('application/json')" },
      { description: 'type is a string', assertion: "expect(typeof new Blob(['x'], { type: 'text/plain' }).type).toBe('string')" },
      { description: 'type is preserved exactly', assertion: "expect(new Blob(['x'], { type: 'text/html' }).type).toBe('text/html')" },
      { description: 'size is unaffected by type', assertion: "expect(new Blob(['hello'], { type: 'text/plain' }).size).toBe(5)" },
    ],
    hints: ['The type option sets the MIME type, it does not affect size.'],
    tags: ['Blob', 'constructor', 'type', 'MIME'],
  },
  {
    slug: 'blob-constructor-3',
    title: 'Blob — empty blob size',
    description: `## Empty Blob\n\n\`new Blob([]).size\` equals **0** because there are no parts.\n\n**Challenge:** Verify that \`new Blob([]).size === 0\`.`,
    category: 'constructor',
    difficulty: 'beginner',
    builtIn: 'Blob',
    initialCode: `// Create an empty Blob\nconst b = new Blob([])\n`,
    solution: `new Blob([]).size`,
    tests: [
      { description: 'empty blob size is 0', assertion: "expect(new Blob([]).size).toBe(0)" },
      { description: 'instanceof Blob', assertion: "expect(new Blob([]) instanceof Blob).toBe(true)" },
      { description: 'type defaults to empty string', assertion: "expect(new Blob([]).type).toBe('')" },
      { description: 'is not null', assertion: "expect(new Blob([])).not.toBeNull()" },
      { description: 'size is 0, not negative', assertion: "expect(new Blob([]).size).toBeGreaterThanOrEqual(0)" },
    ],
    hints: ['An empty parts array produces a zero-byte Blob.'],
    tags: ['Blob', 'constructor', 'size', 'empty'],
  },
  {
    slug: 'blob-constructor-4',
    title: 'Blob — multi-part concatenation',
    description: `## Multi-part Blob\n\nBlob parts are concatenated. \`new Blob(['a', 'b']).size\` equals **2** (1 byte each).\n\n**Challenge:** Verify that \`new Blob(['a', 'b']).size === 2\`.`,
    category: 'constructor',
    difficulty: 'intermediate',
    builtIn: 'Blob',
    initialCode: `// Create a Blob from multiple string parts\nconst b = new Blob(['a', 'b'])\n`,
    solution: `new Blob(['a', 'b']).size`,
    tests: [
      { description: "['a','b'] size is 2", assertion: "expect(new Blob(['a', 'b']).size).toBe(2)" },
      { description: "['foo','bar'] size is 6", assertion: "expect(new Blob(['foo', 'bar']).size).toBe(6)" },
      { description: "['hello',' ','world'] size is 11", assertion: "expect(new Blob(['hello', ' ', 'world']).size).toBe(11)" },
      { description: 'parts are concatenated not nested', assertion: "expect(new Blob(['ab', 'cd']).size).toBe(4)" },
      { description: 'empty part adds 0 bytes', assertion: "expect(new Blob(['a', '']).size).toBe(1)" },
    ],
    hints: ['Blob parts are concatenated in order.'],
    tags: ['Blob', 'constructor', 'size', 'multi-part'],
  },
  {
    slug: 'blob-constructor-5',
    title: "Blob — size of '€' (multi-byte)",
    description: `## Blob UTF-8 Encoding\n\nThe Euro sign \`'€'\` (U+20AC) is encoded as **3 bytes** in UTF-8. Therefore \`new Blob(['€']).size === 3\`.\n\n**Challenge:** Verify that \`new Blob(['€']).size === 3\`.`,
    category: 'constructor',
    difficulty: 'advanced',
    builtIn: 'Blob',
    initialCode: `// Check how many bytes '€' takes in a Blob\nconst b = new Blob(['€'])\n`,
    solution: `new Blob(['€']).size`,
    tests: [
      { description: "size of '€' is 3", assertion: "expect(new Blob(['€']).size).toBe(3)" },
      { description: 'instanceof Blob', assertion: "expect(new Blob(['€']) instanceof Blob).toBe(true)" },
      { description: "size of '€€' is 6", assertion: "expect(new Blob(['€€']).size).toBe(6)" },
      { description: "size of 'A€' is 4", assertion: "expect(new Blob(['A€']).size).toBe(4)" },
      { description: "size type is number", assertion: "expect(typeof new Blob(['€']).size).toBe('number')" },
    ],
    hints: ['Blob stores UTF-8 bytes; € is 3 bytes.'],
    tags: ['Blob', 'constructor', 'size', 'UTF-8', 'multi-byte'],
  },
]
