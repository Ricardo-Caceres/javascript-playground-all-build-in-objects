import type { Exercise } from '@/shared/types/exercises'

export const cryptoRandomExercises: Exercise[] = [
  {
    slug: 'crypto-random-1',
    title: 'crypto — getRandomValues returns same typed array',
    description: `## crypto.getRandomValues()\n\n\`crypto.getRandomValues(typedArray)\` fills the given typed array with cryptographically strong random values and returns the same typed array object.\n\n**Challenge:** Verify that \`getRandomValues\` returns the same array reference that was passed in.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'crypto',
    initialCode: `// Pass in a typed array and check if the same reference is returned\nconst a = new Uint8Array(4)\ncrypto.getRandomValues(a) === a`,
    solution: `const a = new Uint8Array(4)
crypto.getRandomValues(a) === a`,
    tests: [
      { description: 'getRandomValues returns same typed array', assertion: "const a=new Uint8Array(4); expect(crypto.getRandomValues(a)).toBe(a)" },
      { description: 'returned array has correct length', assertion: "expect(result).toBe(8)" },
      { description: 'values are numbers', assertion: "expect(result).toBe('number')" },
      { description: 'values are in 0-255 range', assertion: "const v=crypto.getRandomValues(new Uint8Array(1))[0]; expect(v>=0 && v<=255).toBe(true)" },
      { description: 'works with Uint32Array', assertion: "expect(result).toBe(2)" },
    ],
    hints: ['getRandomValues mutates the input array and also returns it'],
    tags: ['crypto', 'getRandomValues', 'static-method'],
  },
  {
    slug: 'crypto-random-2',
    title: 'crypto — returned array has correct length',
    description: `## crypto.getRandomValues() — length preservation\n\nWhen \`crypto.getRandomValues(typedArray)\` is called, the returned array maintains the same length as the input array.\n\n**Challenge:** Verify that an 8-element array returned from \`getRandomValues\` has length 8.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'crypto',
    initialCode: `// Create an 8-element array and verify the length is preserved\ncrypto.getRandomValues(new Uint8Array(8)).length`,
    solution: `crypto.getRandomValues(new Uint8Array(8)).length`,
    tests: [
      { description: 'returned array has correct length', assertion: "expect(result).toBe(8)" },
      { description: 'getRandomValues returns same typed array', assertion: "const a=new Uint8Array(4); expect(crypto.getRandomValues(a)).toBe(a)" },
      { description: 'values are numbers', assertion: "expect(result).toBe('number')" },
      { description: 'values are in 0-255 range', assertion: "const v=crypto.getRandomValues(new Uint8Array(1))[0]; expect(v>=0 && v<=255).toBe(true)" },
      { description: 'works with Uint32Array', assertion: "expect(result).toBe(2)" },
    ],
    hints: ['getRandomValues fills the array in place without changing its length'],
    tags: ['crypto', 'getRandomValues', 'static-method'],
  },
  {
    slug: 'crypto-random-3',
    title: 'crypto — values are numbers',
    description: `## crypto.getRandomValues() — element types\n\nEach element in the array filled by \`crypto.getRandomValues()\` is a \`number\`.\n\n**Challenge:** Verify that the first element of a random array is a number.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'crypto',
    initialCode: `// Get a random value and check its type\ntypeof crypto.getRandomValues(new Uint8Array(1))[0]`,
    solution: `typeof crypto.getRandomValues(new Uint8Array(1))[0]`,
    tests: [
      { description: 'values are numbers', assertion: "expect(result).toBe('number')" },
      { description: 'getRandomValues returns same typed array', assertion: "const a=new Uint8Array(4); expect(crypto.getRandomValues(a)).toBe(a)" },
      { description: 'returned array has correct length', assertion: "expect(result).toBe(8)" },
      { description: 'values are in 0-255 range', assertion: "const v=crypto.getRandomValues(new Uint8Array(1))[0]; expect(v>=0 && v<=255).toBe(true)" },
      { description: 'works with Uint32Array', assertion: "expect(result).toBe(2)" },
    ],
    hints: ['Typed array elements are automatically numbers'],
    tags: ['crypto', 'getRandomValues', 'static-method'],
  },
  {
    slug: 'crypto-random-4',
    title: 'crypto — values are in 0-255 range',
    description: `## crypto.getRandomValues() — value range for Uint8Array\n\nFor a \`Uint8Array\`, values returned by \`crypto.getRandomValues()\` are in the range [0, 255].\n\n**Challenge:** Verify that a random Uint8Array value is between 0 and 255 inclusive.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'crypto',
    initialCode: `// Get a random value and check its range\nconst v = crypto.getRandomValues(new Uint8Array(1))[0]\nv >= 0 && v <= 255`,
    solution: `const v = crypto.getRandomValues(new Uint8Array(1))[0]
v >= 0 && v <= 255`,
    tests: [
      { description: 'values are in 0-255 range', assertion: "const v=crypto.getRandomValues(new Uint8Array(1))[0]; expect(v>=0 && v<=255).toBe(true)" },
      { description: 'getRandomValues returns same typed array', assertion: "const a=new Uint8Array(4); expect(crypto.getRandomValues(a)).toBe(a)" },
      { description: 'returned array has correct length', assertion: "expect(result).toBe(8)" },
      { description: 'values are numbers', assertion: "expect(result).toBe('number')" },
      { description: 'works with Uint32Array', assertion: "expect(result).toBe(2)" },
    ],
    hints: ['Uint8Array holds 8-bit unsigned integers, so values are 0-255'],
    tags: ['crypto', 'getRandomValues', 'static-method'],
  },
  {
    slug: 'crypto-random-5',
    title: 'crypto — works with Uint32Array',
    description: `## crypto.getRandomValues() — other typed array types\n\n\`crypto.getRandomValues()\` works with various typed array types, including \`Uint32Array\`.\n\n**Challenge:** Verify that \`getRandomValues\` can fill a \`Uint32Array\` and returns an array of length 2.`,
    category: 'static-method',
    difficulty: 'advanced',
    builtIn: 'crypto',
    initialCode: `// Use getRandomValues with Uint32Array\ncrypto.getRandomValues(new Uint32Array(2)).length`,
    solution: `crypto.getRandomValues(new Uint32Array(2)).length`,
    tests: [
      { description: 'works with Uint32Array', assertion: "expect(result).toBe(2)" },
      { description: 'getRandomValues returns same typed array', assertion: "const a=new Uint8Array(4); expect(crypto.getRandomValues(a)).toBe(a)" },
      { description: 'returned array has correct length', assertion: "expect(result).toBe(8)" },
      { description: 'values are numbers', assertion: "expect(result).toBe('number')" },
      { description: 'values are in 0-255 range', assertion: "const v=crypto.getRandomValues(new Uint8Array(1))[0]; expect(v>=0 && v<=255).toBe(true)" },
    ],
    hints: ['getRandomValues supports multiple typed array constructors'],
    tags: ['crypto', 'getRandomValues', 'static-method'],
  },
]
