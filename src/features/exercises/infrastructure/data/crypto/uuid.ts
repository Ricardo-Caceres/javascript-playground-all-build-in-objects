import type { Exercise } from '@/shared/types/exercises'

export const cryptoUuidExercises: Exercise[] = [
  {
    slug: 'crypto-uuid-1',
    title: 'crypto — randomUUID returns a string',
    description: `## crypto.randomUUID()\n\n\`crypto.randomUUID()\` generates a random UUID (Universally Unique Identifier) in string format.\n\n**Challenge:** Verify that \`crypto.randomUUID()\` returns a string.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'crypto',
    initialCode: `// Check the type of the returned UUID\ntypeof crypto.randomUUID()`,
    solution: `typeof crypto.randomUUID()`,
    tests: [
      { description: 'randomUUID returns a string', assertion: "expect(result).toBe('string')" },
      { description: 'UUID has length 36', assertion: "expect(result).toBe(36)" },
      { description: 'UUID contains dashes', assertion: "expect(result).toBe(true)" },
      { description: 'UUID matches format', assertion: "expect(crypto.randomUUID()).toMatch(/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/)" },
      { description: 'consecutive UUIDs are different', assertion: "expect(crypto.randomUUID()).not.toBe(crypto.randomUUID())" },
    ],
    hints: ['UUIDs are formatted as text strings'],
    tags: ['crypto', 'randomUUID', 'static-method'],
  },
  {
    slug: 'crypto-uuid-2',
    title: 'crypto — UUID has length 36',
    description: `## crypto.randomUUID() — string length\n\nA UUID string has a fixed length of 36 characters (including dashes): \`xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx\`.\n\n**Challenge:** Verify that a UUID string has length 36.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'crypto',
    initialCode: `// Get the length of a UUID\ncrypto.randomUUID().length`,
    solution: `crypto.randomUUID().length`,
    tests: [
      { description: 'UUID has length 36', assertion: "expect(result).toBe(36)" },
      { description: 'randomUUID returns a string', assertion: "expect(result).toBe('string')" },
      { description: 'UUID contains dashes', assertion: "expect(result).toBe(true)" },
      { description: 'UUID matches format', assertion: "expect(crypto.randomUUID()).toMatch(/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/)" },
      { description: 'consecutive UUIDs are different', assertion: "expect(crypto.randomUUID()).not.toBe(crypto.randomUUID())" },
    ],
    hints: ['UUID format is 8-4-4-4-12 hex digits separated by dashes'],
    tags: ['crypto', 'randomUUID', 'static-method'],
  },
  {
    slug: 'crypto-uuid-3',
    title: 'crypto — UUID contains dashes',
    description: `## crypto.randomUUID() — dash format\n\nUUIDs are formatted with dashes at specific positions to separate groups of hex digits.\n\n**Challenge:** Verify that a UUID string contains dash characters.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'crypto',
    initialCode: `// Check if the UUID contains dashes\ncrypto.randomUUID().includes('-')`,
    solution: `crypto.randomUUID().includes('-')`,
    tests: [
      { description: 'UUID contains dashes', assertion: "expect(result).toBe(true)" },
      { description: 'randomUUID returns a string', assertion: "expect(result).toBe('string')" },
      { description: 'UUID has length 36', assertion: "expect(result).toBe(36)" },
      { description: 'UUID matches format', assertion: "expect(crypto.randomUUID()).toMatch(/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/)" },
      { description: 'consecutive UUIDs are different', assertion: "expect(crypto.randomUUID()).not.toBe(crypto.randomUUID())" },
    ],
    hints: ['A valid UUID has dashes at positions 8, 13, 18, and 23'],
    tags: ['crypto', 'randomUUID', 'static-method'],
  },
  {
    slug: 'crypto-uuid-4',
    title: 'crypto — UUID matches format',
    description: `## crypto.randomUUID() — format validation\n\nA UUID has a specific format: \`xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx\` where x is a hex digit and y is 8, 9, a, or b.\n\n**Challenge:** Verify that a UUID matches the standard UUID v4 format.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'crypto',
    initialCode: `// Check if the UUID matches the correct format\ncrypto.randomUUID().match(/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/)`,
    solution: `crypto.randomUUID().match(/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/)`,
    tests: [
      { description: 'UUID matches format', assertion: "expect(crypto.randomUUID()).toMatch(/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/)" },
      { description: 'randomUUID returns a string', assertion: "expect(result).toBe('string')" },
      { description: 'UUID has length 36', assertion: "expect(result).toBe(36)" },
      { description: 'UUID contains dashes', assertion: "expect(result).toBe(true)" },
      { description: 'consecutive UUIDs are different', assertion: "expect(crypto.randomUUID()).not.toBe(crypto.randomUUID())" },
    ],
    hints: ['The third group always starts with 4 to indicate version 4'],
    tags: ['crypto', 'randomUUID', 'static-method'],
  },
  {
    slug: 'crypto-uuid-5',
    title: 'crypto — consecutive UUIDs are different',
    description: `## crypto.randomUUID() — uniqueness\n\nEach call to \`crypto.randomUUID()\` generates a different UUID. Two consecutive UUIDs should not be equal.\n\n**Challenge:** Verify that two UUIDs generated in succession are different.`,
    category: 'static-method',
    difficulty: 'advanced',
    builtIn: 'crypto',
    initialCode: `// Generate two UUIDs and check they are different\ncrypto.randomUUID() !== crypto.randomUUID()`,
    solution: `crypto.randomUUID() !== crypto.randomUUID()`,
    tests: [
      { description: 'consecutive UUIDs are different', assertion: "expect(crypto.randomUUID()).not.toBe(crypto.randomUUID())" },
      { description: 'randomUUID returns a string', assertion: "expect(result).toBe('string')" },
      { description: 'UUID has length 36', assertion: "expect(result).toBe(36)" },
      { description: 'UUID contains dashes', assertion: "expect(result).toBe(true)" },
      { description: 'UUID matches format', assertion: "expect(crypto.randomUUID()).toMatch(/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/)" },
    ],
    hints: ['UUIDs are designed to be statistically unique, so collisions are astronomically unlikely'],
    tags: ['crypto', 'randomUUID', 'static-method', 'uniqueness'],
  },
]
