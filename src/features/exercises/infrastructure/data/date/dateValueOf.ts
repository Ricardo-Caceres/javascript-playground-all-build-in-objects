import type { Exercise } from '@/shared/types/exercises'

export const dateValueOfExercises: Exercise[] = [
  {
    slug: 'date-valueof-1',
    title: 'valueOf() — epoch returns 0',
    description: `## Date.prototype.valueOf()\n\nReturns the same primitive value as \`getTime()\` — milliseconds since the Unix epoch.\n\n**Challenge:** Verify that \`new Date(0).valueOf()\` returns 0.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Date',
    method: 'valueOf',
    initialCode: `// Use new Date(0).valueOf()\n`,
    solution: `new Date(0).valueOf()`,
    tests: [
      { description: 'result returns 0', assertion: "expect(result).toBe(0)" },
      { description: 'result is a number', assertion: "expect(typeof result).toBe('number')" },
      { description: 'result === 0', assertion: "expect(result === 0).toBeTruthy()" },
      { description: 'result is integer', assertion: "expect(Number.isInteger(result)).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(result)).toBeTruthy()" },
    ],
    hints: ['valueOf() returns the numeric timestamp — just like getTime()'],
    tags: ['Date', 'valueOf', 'instance-method', 'epoch'],
  },
  {
    slug: 'date-valueof-2',
    title: 'valueOf() — same as getTime()',
    description: `## Date.prototype.valueOf()\n\n\`valueOf()\` and \`getTime()\` return the same number.\n\n**Challenge:** Verify that \`new Date(0).valueOf() === new Date(0).getTime()\`.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Date',
    method: 'valueOf',
    initialCode: `// Compare new Date(0).valueOf() and new Date(0).getTime()\n`,
    solution: `new Date(0).valueOf() === new Date(0).getTime()`,
    tests: [
      { description: 'valueOf() === getTime() for epoch', assertion: "expect(result === new Date(0).getTime()).toBeTruthy()" },
      { description: 'valueOf() === getTime() for 2024', assertion: "expect(new Date('2024-01-15T00:00:00.000Z').valueOf() === new Date('2024-01-15T00:00:00.000Z').getTime()).toBeTruthy()" },
      { description: 'both return numbers', assertion: "expect(typeof result).toBe('number')" },
      { description: 'epoch is 0', assertion: "expect(result).toBe(0)" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(result)).toBeTruthy()" },
    ],
    hints: ['valueOf() is the JavaScript coercion hook — it is called whenever a Date is used as a number'],
    tags: ['Date', 'valueOf', 'instance-method', 'getTime'],
  },
  {
    slug: 'date-valueof-3',
    title: 'valueOf() — result is a number',
    description: `## Date.prototype.valueOf()\n\n\`valueOf()\` returns a number.\n\n**Challenge:** Verify that \`typeof new Date(0).valueOf()\` returns \`'number'\`.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Date',
    method: 'valueOf',
    initialCode: `// Use typeof new Date(0).valueOf()\n`,
    solution: `typeof new Date(0).valueOf()`,
    tests: [
      { description: "typeof result is 'number'", assertion: "expect(typeof result).toBe('number')" },
      { description: 'result is integer', assertion: "expect(Number.isInteger(result)).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(result)).toBeTruthy()" },
      { description: 'result is not NaN', assertion: "expect(!isNaN(result)).toBeTruthy()" },
      { description: 'result === 0 for epoch', assertion: "expect(result).toBe(0)" },
    ],
    hints: ['valueOf() returns a number — the timestamp in ms since the epoch'],
    tags: ['Date', 'valueOf', 'instance-method', 'typeof'],
  },
  {
    slug: 'date-valueof-4',
    title: 'valueOf() — positive for modern dates',
    description: `## Date.prototype.valueOf()\n\nFor any date after 1970, \`valueOf()\` returns a positive number.\n\n**Challenge:** Verify that \`new Date('2024-01-15T00:00:00.000Z').valueOf() > 0\`.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Date',
    method: 'valueOf',
    initialCode: `// Use new Date('2024-01-15T00:00:00.000Z').valueOf()\n`,
    solution: `new Date('2024-01-15T00:00:00.000Z').valueOf() > 0`,
    tests: [
      { description: 'valueOf() > 0 for 2024', assertion: "expect(result).toBeTruthy()" },
      { description: 'result is a number', assertion: "expect(typeof new Date('2024-01-15T00:00:00.000Z').valueOf()).toBe('number')" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(new Date('2024-01-15T00:00:00.000Z').valueOf())).toBeTruthy()" },
      { description: 'result is not NaN', assertion: "expect(!isNaN(new Date('2024-01-15T00:00:00.000Z').valueOf())).toBeTruthy()" },
      { description: 'equals getTime()', assertion: "expect(new Date('2024-01-15T00:00:00.000Z').valueOf()).toBe(new Date('2024-01-15T00:00:00.000Z').getTime())" },
    ],
    hints: ['Dates after 1970 have positive timestamps'],
    tags: ['Date', 'valueOf', 'instance-method'],
  },
  {
    slug: 'date-valueof-5',
    title: 'valueOf() — used in arithmetic',
    description: `## Date.prototype.valueOf()\n\nWhen you subtract two Dates, JavaScript calls \`valueOf()\` automatically, returning the difference in ms.\n\n**Challenge:** Verify that subtracting two Dates gives a number (the difference in ms).`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Date',
    method: 'valueOf',
    initialCode: `// Subtract two Date objects and check the result\n`,
    solution: `new Date('2024-01-15T00:00:01.000Z') - new Date('2024-01-15T00:00:00.000Z')`,
    tests: [
      { description: 'subtracting two dates gives 1000 ms', assertion: "expect(result).toBe(1000)" },
      { description: 'result is a number', assertion: "expect(typeof (result)).toBe('number')" },
      { description: 'result > 0 (later - earlier)', assertion: "expect(result > 0).toBeTruthy()" },
      { description: 'result is integer', assertion: "expect(Number.isInteger(result)).toBeTruthy()" },
      { description: 'valueOf() equals getTime()', assertion: "expect(result).toBe(new Date(0).getTime())" },
    ],
    hints: ['Date subtraction implicitly calls valueOf() on both operands, yielding the ms difference'],
    tags: ['Date', 'valueOf', 'instance-method', 'arithmetic'],
  },
]
