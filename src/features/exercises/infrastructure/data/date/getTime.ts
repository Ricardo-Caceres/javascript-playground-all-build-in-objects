import type { Exercise } from '@/shared/types/exercises'

export const dateGetTimeExercises: Exercise[] = [
  {
    slug: 'date-gettime-1',
    title: 'getTime() — epoch returns 0',
    description: `## Date.prototype.getTime()\n\nReturns milliseconds since the Unix epoch (Jan 1, 1970 00:00:00 UTC). For the epoch itself this is 0.\n\n**Challenge:** Verify that \`new Date(0).getTime()\` returns 0.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Date',
    method: 'getTime',
    initialCode: `// Use new Date(0).getTime()\n`,
    solution: `new Date(0).getTime()`,
    tests: [
      { description: 'result returns 0', assertion: "expect(result).toBe(0)" },
      { description: 'result is a number', assertion: "expect(typeof result).toBe('number')" },
      { description: 'result === 0', assertion: "expect(result === 0).toBeTruthy()" },
      { description: 'result is integer', assertion: "expect(Number.isInteger(result)).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(result)).toBeTruthy()" },
    ],
    hints: ['new Date(0) creates the Unix epoch; its getTime() is exactly 0'],
    tags: ['Date', 'getTime', 'instance-method', 'epoch'],
  },
  {
    slug: 'date-gettime-2',
    title: 'getTime() — 2024 date is positive',
    description: `## Date.prototype.getTime()\n\nAny date after 1970 returns a positive timestamp.\n\n**Challenge:** Verify that \`new Date('2024-01-15T00:00:00.000Z').getTime() > 0\`.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Date',
    method: 'getTime',
    initialCode: `// Use new Date('2024-01-15T00:00:00.000Z').getTime()\n`,
    solution: `new Date('2024-01-15T00:00:00.000Z').getTime() > 0`,
    tests: [
      { description: "getTime() > 0 for 2024 date", assertion: "expect(result).toBeTruthy()" },
      { description: 'result is a number', assertion: "expect(typeof new Date('2024-01-15T00:00:00.000Z').getTime()).toBe('number')" },
      { description: 'result is not NaN', assertion: "expect(!isNaN(new Date('2024-01-15T00:00:00.000Z').getTime())).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(new Date('2024-01-15T00:00:00.000Z').getTime())).toBeTruthy()" },
      { description: 'result is integer', assertion: "expect(Number.isInteger(new Date('2024-01-15T00:00:00.000Z').getTime())).toBeTruthy()" },
    ],
    hints: ['Any ISO string date after 1970 gives a positive getTime() result'],
    tags: ['Date', 'getTime', 'instance-method'],
  },
  {
    slug: 'date-gettime-3',
    title: 'getTime() — result is a number',
    description: `## Date.prototype.getTime()\n\n\`getTime()\` always returns a number (the millisecond timestamp).\n\n**Challenge:** Verify that \`typeof new Date(0).getTime()\` returns \`'number'\`.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Date',
    method: 'getTime',
    initialCode: `// Use typeof new Date(0).getTime()\n`,
    solution: `typeof new Date(0).getTime()`,
    tests: [
      { description: "typeof result is 'number'", assertion: "expect(typeof result).toBe('number')" },
      { description: 'result is integer', assertion: "expect(Number.isInteger(result)).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(result)).toBeTruthy()" },
      { description: 'result is not NaN', assertion: "expect(!isNaN(result)).toBeTruthy()" },
      { description: 'result >= 0 for epoch', assertion: "expect(result >= 0).toBeTruthy()" },
    ],
    hints: ['getTime() returns a number primitive — the timestamp in milliseconds'],
    tags: ['Date', 'getTime', 'instance-method', 'typeof'],
  },
  {
    slug: 'date-gettime-4',
    title: 'getTime() — same date, same timestamp',
    description: `## Date.prototype.getTime()\n\nTwo Date objects created with the same value have equal timestamps.\n\n**Challenge:** Verify that two dates created from the same string have the same \`getTime()\` value.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Date',
    method: 'getTime',
    initialCode: `// Create two Date objects from the same string and compare getTime()\n`,
    solution: `new Date('2024-01-15T00:00:00.000Z').getTime() === new Date('2024-01-15T00:00:00.000Z').getTime()`,
    tests: [
      { description: 'same date string gives equal getTime()', assertion: "expect(result).toBeTruthy()" },
      { description: 'epoch dates are equal', assertion: "expect(result === result).toBeTruthy()" },
      { description: 'different dates differ', assertion: "expect(new Date('2024-01-15T00:00:00.000Z').getTime() !== new Date('2025-01-15T00:00:00.000Z').getTime()).toBeTruthy()" },
      { description: 'result is a number', assertion: "expect(typeof new Date('2024-01-15T00:00:00.000Z').getTime()).toBe('number')" },
      { description: 'later date has larger getTime()', assertion: "expect(new Date('2025-01-15T00:00:00.000Z').getTime() > new Date('2024-01-15T00:00:00.000Z').getTime()).toBeTruthy()" },
    ],
    hints: ['getTime() lets you compare dates numerically — the same instant always yields the same ms value'],
    tags: ['Date', 'getTime', 'instance-method', 'comparison'],
  },
  {
    slug: 'date-gettime-5',
    title: 'getTime() — used for date comparison',
    description: `## Date.prototype.getTime()\n\nCompare two dates by comparing their \`getTime()\` values.\n\n**Challenge:** Verify that the 2024 date has a smaller \`getTime()\` than the 2025 date.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Date',
    method: 'getTime',
    initialCode: `// Compare getTime() of two dates\n`,
    solution: `new Date('2024-01-15T00:00:00.000Z').getTime() < new Date('2025-01-15T00:00:00.000Z').getTime()`,
    tests: [
      { description: '2024 < 2025 by getTime()', assertion: "expect(result).toBeTruthy()" },
      { description: 'epoch < 2024 by getTime()', assertion: "expect(result < new Date('2024-01-15T00:00:00.000Z').getTime()).toBeTruthy()" },
      { description: 'result is a number', assertion: "expect(typeof new Date('2024-01-15T00:00:00.000Z').getTime()).toBe('number')" },
      { description: 'result > 0 for 2024', assertion: "expect(result).toBeTruthy()" },
      { description: 'difference is positive', assertion: "expect(new Date('2025-01-15T00:00:00.000Z').getTime() - result).toBeTruthy()" },
    ],
    hints: ['Subtracting or comparing getTime() values is the standard way to order or diff two dates'],
    tags: ['Date', 'getTime', 'instance-method', 'comparison'],
  },
]
