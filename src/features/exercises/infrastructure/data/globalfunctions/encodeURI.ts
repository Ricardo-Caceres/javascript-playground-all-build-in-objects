import type { Exercise } from '@/shared/types/exercises'

export const globalEncodeURIExercises: Exercise[] = [
  {
    slug: 'global-encodeuri-1',
    title: 'encodeURI — space encoding',
    description: `## encodeURI\n\n\`encodeURI('hello world')\` encodes the space as \`%20\`.\n\n**Challenge:** Verify the encoding.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'GlobalFunctions',
    method: 'encodeURI',
    initialCode: `encodeURI('hello world')`,
    solution: `encodeURI('hello world') === 'hello%20world'`,
    tests: [
      { description: "result", assertion:"expect(encodeURI('hello world')).toBe('hello%20world')" },
      { description: 'result is a string', assertion:"expect(typeof encodeURI('hello world')).toBe('string')" },
      { description: 'contains %20', assertion:"expect(encodeURI('hello world')).toContain('%20')" },
      { description: 'no space in result', assertion:"expect(encodeURI('hello world').includes(' ')).toBe(false)" },
      { description: 'is truthy', assertion:"expect(encodeURI('hello world')).toBeTruthy()" },
    ],
    hints: ['encodeURI encodes spaces as %20.'],
    tags: ['globalfunctions', 'encodeURI', 'url'],
    usageExample: {
      code: `encodeURI('hello world')   // → 'hello%20world'`,
      explanation: {
        en: 'encodeURI() percent-encodes characters that are not valid in a URI.',
        es: 'encodeURI() codifica en porcentaje los caracteres no válidos en una URI.',
      },
    },
  },
  {
    slug: 'global-encodeuri-2',
    title: 'encodeURI — URL with no encoding needed',
    description: `## encodeURI No-Op\n\n\`encodeURI('https://example.com/path')\` does not need any encoding.\n\n**Challenge:** Verify the result is unchanged.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'GlobalFunctions',
    method: 'encodeURI',
    initialCode: `encodeURI('https://example.com/path')`,
    solution: `encodeURI('https://example.com/path') === 'https://example.com/path'`,
    tests: [
      { description: "URL is unchanged", assertion:"expect(encodeURI('https://example.com/path')).toBe('https://example.com/path')" },
      { description: 'result is a string', assertion:"expect(typeof encodeURI('https://example.com/path')).toBe('string')" },
      { description: 'contains https', assertion:"expect(encodeURI('https://example.com/path')).toContain('https')" },
      { description: 'contains ://', assertion:"expect(encodeURI('https://example.com/path')).toContain('://')" },
      { description: 'is truthy', assertion:"expect(encodeURI('https://example.com/path')).toBeTruthy()" },
    ],
    hints: ['encodeURI does not encode characters that are valid in a URL like ://.'],
    tags: ['globalfunctions', 'encodeURI', 'url'],
    usageExample: {
      code: `encodeURI('https://example.com/path')   // unchanged
// :// and / are valid URI characters`,
      explanation: {
        en: 'encodeURI() preserves characters valid in a URI structure like :, /, ?, and &.',
        es: 'encodeURI() preserva caracteres válidos en la estructura de URI como :, /, ? y &.',
      },
    },
  },
  {
    slug: 'global-encodeuri-3',
    title: 'encodeURI — ampersand not encoded',
    description: `## encodeURI Preserves &\n\n\`encodeURI('a=1&b=2')\` does not encode \`&\` (it is a valid URI character).\n\n**Challenge:** Verify \`&\` is preserved.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'GlobalFunctions',
    method: 'encodeURI',
    initialCode: `encodeURI('a=1&b=2')`,
    solution: `encodeURI('a=1&b=2') === 'a=1&b=2'`,
    tests: [
      { description: "& is not encoded", assertion:"expect(encodeURI('a=1&b=2')).toBe('a=1&b=2')" },
      { description: 'result contains &', assertion:"expect(encodeURI('a=1&b=2')).toContain('&')" },
      { description: 'result is a string', assertion:"expect(typeof encodeURI('a=1&b=2')).toBe('string')" },
      { description: 'result contains =', assertion:"expect(encodeURI('a=1&b=2')).toContain('=')" },
      { description: 'is truthy', assertion:"expect(encodeURI('a=1&b=2')).toBeTruthy()" },
    ],
    hints: ['encodeURI preserves characters that are valid in a full URI including & = and ?.'],
    tags: ['globalfunctions', 'encodeURI', 'url'],
    usageExample: {
      code: `encodeURI('a=1&b=2')   // → 'a=1&b=2'
// & and = are valid URI characters`,
      explanation: {
        en: 'encodeURI() does not encode & or = because they are valid URI characters.',
        es: 'encodeURI() no codifica & ni = porque son caracteres válidos de URI.',
      },
    },
  },
  {
    slug: 'global-encodeuri-4',
    title: 'encodeURI — returns a string',
    description: `## encodeURI typeof\n\n\`typeof encodeURI('test')\` is \`'string'\`.\n\n**Challenge:** Verify the type.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'GlobalFunctions',
    method: 'encodeURI',
    initialCode: `typeof encodeURI('test')`,
    solution: `typeof encodeURI('test') === 'string'`,
    tests: [
      { description: "typeof encodeURI is 'string'", assertion:"expect(typeof encodeURI('test')).toBe('string')" },
      { description: "encodeURI('test') === 'test'", assertion:"expect(encodeURI('test')).toBe('test')" },
      { description: 'is truthy', assertion:"expect(encodeURI('test')).toBeTruthy()" },
      { description: 'has length > 0', assertion:"expect(encodeURI('test').length > 0).toBe(true)" },
      { description: 'not null', assertion:"expect(encodeURI('test') !== null).toBe(true)" },
    ],
    hints: ['encodeURI always returns a string.'],
    tags: ['globalfunctions', 'encodeURI', 'typeof'],
    usageExample: {
      code: `typeof encodeURI('test')   // → 'string'`,
      explanation: {
        en: 'encodeURI() always returns a string.',
        es: 'encodeURI() siempre devuelve una cadena.',
      },
    },
  },
  {
    slug: 'global-encodeuri-5',
    title: 'encodeURI — non-ASCII encoded',
    description: `## encodeURI Non-ASCII\n\n\`encodeURI('café')\` encodes non-ASCII characters, so the result differs from the original.\n\n**Challenge:** Verify non-ASCII characters are encoded.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'GlobalFunctions',
    method: 'encodeURI',
    initialCode: `encodeURI('café') !== 'café'`,
    solution: `encodeURI('café') !== 'café'`,
    tests: [
      { description: "non-ASCII is encoded", assertion:"expect(result).toBe(true)" },
      { description: 'result is a string', assertion:"expect(typeof encodeURI('café')).toBe('string')" },
      { description: 'result is truthy', assertion:"expect(encodeURI('café')).toBeTruthy()" },
      { description: 'result contains %', assertion:"expect(encodeURI('café')).toContain('%')" },
      { description: 'result is longer', assertion:"expect(encodeURI('café').length > 'café'.length).toBe(true)" },
    ],
    hints: ['Non-ASCII characters are percent-encoded as their UTF-8 byte sequences.'],
    tags: ['globalfunctions', 'encodeURI', 'unicode'],
    usageExample: {
      code: `encodeURI('caf\u00e9') !== 'caf\u00e9'  // → true
// non-ASCII characters are percent-encoded as UTF-8 bytes`,
      explanation: {
        en: 'encodeURI() percent-encodes non-ASCII characters as their UTF-8 byte sequences.',
        es: 'encodeURI() codifica en porcentaje los caracteres no-ASCII como sus bytes UTF-8.',
      },
    },
  },
]
