import type { Exercise } from '@/shared/types/exercises'

export const mapSizeExercises: Exercise[] = [
  {
    slug: 'map-size-1',
    title: 'Map size — empty Map',
    description: `## Map.prototype.size\n\nThe \`size\` property returns the number of key-value pairs. An empty Map has size 0.\n\n**Challenge:** Verify that \`new Map().size\` equals 0.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'Map',
    method: 'size',
    initialCode: `// Use new Map().size\n`,
    solution: `new Map().size`,
    tests: [
      { description: 'result equals 0', assertion:'expect(result).toBe(0)' },
      { description: 'size is a number', assertion:"expect(typeof result).toBe('number')" },
      { description: 'size is 0 (falsy)', assertion:'expect(result).toBeFalsy()' },
      { description: 'size is not negative', assertion:'expect(result >= 0).toBe(true)' },
      { description: 'size equals 0 strictly', assertion:'expect(result === 0).toBe(true)' },
    ],
    hints: ['An empty Map has size 0'],
    tags: ['Map', 'size', 'property'],
  },
  {
    slug: 'map-size-2',
    title: 'Map size — after set',
    description: `## Map.prototype.size\n\nAdding an entry with \`set()\` increases the size.\n\n**Challenge:** Verify that after calling set(), size equals 1.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'Map',
    method: 'size',
    initialCode: `// Create a Map and call set()\nconst m = new Map()\nm.set('a', 1)\n`,
    solution: `const m = new Map(); m.set('a', 1); m.size`,
    tests: [
      { description: 'size is 1 after one set', assertion:"const m1 = new Map(); m1.set('a', 1); expect(m1.size).toBe(1)" },
      { description: 'size is 2 after two sets', assertion:"const m2 = new Map(); m2.set('a',1); m2.set('b',2); expect(m2.size).toBe(2)" },
      { description: 'size is a number', assertion:"const m3 = new Map(); m3.set('x',0); expect(typeof m3.size).toBe('number')" },
      { description: 'size is truthy after set', assertion:"const m4 = new Map(); m4.set('a',1); expect(m4.size).toBeTruthy()" },
      { description: 'size returns correct count', assertion:"const m5 = new Map([['a',1],['b',2],['c',3]]); expect(m5.size).toBe(3)" },
    ],
    hints: ['Each unique key added with set() increases size by 1'],
    tags: ['Map', 'size', 'set'],
  },
  {
    slug: 'map-size-3',
    title: 'Map size — two entries',
    description: `## Map.prototype.size\n\nAfter adding two different keys, size equals 2.\n\n**Challenge:** Verify that \`new Map([['a',1],['b',2]]).size\` equals 2.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'Map',
    method: 'size',
    initialCode: `// Use new Map([['a',1],['b',2]]).size\n`,
    solution: `new Map([['a',1],['b',2]]).size`,
    tests: [
      { description: "size is 2 for two entries", assertion:"expect(result).toBe(2)" },
      { description: 'size is a number', assertion:"expect(typeof result).toBe('number')" },
      { description: 'size is truthy', assertion:"expect(result).toBeTruthy()" },
      { description: 'size equals number of unique keys', assertion:"expect(new Map([['a',1],['a',2]]).size).toBe(1)" },
      { description: 'duplicate key does not increase size', assertion:"const m = new Map(); m.set('a',1); m.set('a',2); expect(m.size).toBe(1)" },
    ],
    hints: ['Setting the same key twice keeps size at 1'],
    tags: ['Map', 'size', 'duplicate'],
  },
  {
    slug: 'map-size-4',
    title: 'Map size — after delete',
    description: `## Map.prototype.size\n\nDeleting an entry decreases the size.\n\n**Challenge:** Verify that after delete(), size decreases.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'Map',
    method: 'size',
    initialCode: `// Create a Map, add entries, then delete one\nconst m = new Map([['a',1],['b',2]])\nm.delete('a')\n`,
    solution: `const m = new Map([['a',1],['b',2]]); m.delete('a'); m.size`,
    tests: [
      { description: 'size decreases after delete', assertion:"const m1 = new Map([['a',1],['b',2]]); m1.delete('a'); expect(m1.size).toBe(1)" },
      { description: 'size is 0 after deleting all', assertion:"const m2 = new Map([['a',1]]); m2.delete('a'); expect(m2.size).toBe(0)" },
      { description: 'size unchanged when key not found', assertion:"const m3 = new Map([['a',1]]); m3.delete('x'); expect(m3.size).toBe(1)" },
      { description: 'size before delete is 2', assertion:"const m4 = new Map([['a',1],['b',2]]); expect(m4.size).toBe(2)" },
      { description: 'size after clear is 0', assertion:"const m5 = new Map([['a',1],['b',2]]); m5.clear(); expect(m5.size).toBe(0)" },
    ],
    hints: ['delete() removes the key and decreases the size by 1'],
    tags: ['Map', 'size', 'delete'],
  },
  {
    slug: 'map-size-5',
    title: 'Map size — is a number',
    description: `## Map.prototype.size\n\nThe \`size\` property is always a non-negative integer.\n\n**Challenge:** Verify that \`typeof new Map().size\` is 'number'.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'Map',
    method: 'size',
    initialCode: `// Use typeof new Map().size\n`,
    solution: `typeof new Map().size`,
    tests: [
      { description: "typeof size is 'number'", assertion:"expect(typeof result).toBe('number')" },
      { description: 'size is not null', assertion:'expect(result !== null).toBe(true)' },
      { description: 'size is not undefined', assertion:'expect(result !== undefined).toBe(true)' },
      { description: 'size is non-negative', assertion:'expect(result >= 0).toBe(true)' },
      { description: 'size is an integer', assertion:'expect(Number.isInteger(result)).toBe(true)' },
    ],
    hints: ['size is always a non-negative integer'],
    tags: ['Map', 'size', 'typeof'],
  },
]
