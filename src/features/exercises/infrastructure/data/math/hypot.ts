import type { Exercise } from '@/shared/types/exercises'

export const mathHypotExercises: Exercise[] = [
  {
    slug: 'math-hypot-1',
    title: 'Math.hypot() — 3-4-5 Triangle',
    description: `## Math.hypot()\n\nReturns the square root of the sum of squares of its arguments.\n\n**Challenge:** Use \`Math.hypot(3, 4)\` to compute the hypotenuse of a 3-4-5 right triangle.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'hypot',
    initialCode: `// Use Math.hypot()\n`,
    solution: `Math.hypot(3, 4)`,
    tests: [
      { description: 'Math.hypot(3, 4) returns 5', assertion: "expect(Math.hypot(3, 4)).toBe(5)" },
      { description: 'result is a number', assertion: "expect(typeof Math.hypot(3, 4)).toBe('number')" },
      { description: 'Math.hypot(3, 4) === 5', assertion: "expect(Math.hypot(3, 4) === 5).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.hypot(3, 4))).toBeTruthy()" },
      { description: 'difference from 5 is less than 0.001', assertion: "expect(Math.abs(Math.hypot(3, 4) - 5) < 0.001).toBeTruthy()" },
    ],
    hints: ['The 3-4-5 Pythagorean triple: sqrt(9 + 16) = sqrt(25) = 5'],
    tags: ['Math', 'hypot', 'static-method'],
  },
  {
    slug: 'math-hypot-2',
    title: 'Math.hypot() — 5-12-13 Triangle',
    description: `## Math.hypot()\n\nReturns the square root of the sum of squares of its arguments.\n\n**Challenge:** Use \`Math.hypot(5, 12)\` to compute the hypotenuse of a 5-12-13 right triangle.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'hypot',
    initialCode: `// Use Math.hypot()\n`,
    solution: `Math.hypot(5, 12)`,
    tests: [
      { description: 'Math.hypot(5, 12) returns 13', assertion: "expect(Math.hypot(5, 12)).toBe(13)" },
      { description: 'result is a number', assertion: "expect(typeof Math.hypot(5, 12)).toBe('number')" },
      { description: 'Math.hypot(5, 12) === 13', assertion: "expect(Math.hypot(5, 12) === 13).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.hypot(5, 12))).toBeTruthy()" },
      { description: 'difference from 13 is less than 0.001', assertion: "expect(Math.abs(Math.hypot(5, 12) - 13) < 0.001).toBeTruthy()" },
    ],
    hints: ['The 5-12-13 Pythagorean triple: sqrt(25 + 144) = sqrt(169) = 13'],
    tags: ['Math', 'hypot', 'static-method'],
  },
  {
    slug: 'math-hypot-3',
    title: 'Math.hypot() — Single Zero',
    description: `## Math.hypot()\n\nReturns the square root of the sum of squares of its arguments.\n\n**Challenge:** Verify that \`Math.hypot(0)\` returns 0.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'hypot',
    initialCode: `// Use Math.hypot()\n`,
    solution: `Math.hypot(0)`,
    tests: [
      { description: 'Math.hypot(0) returns 0', assertion: "expect(Math.hypot(0)).toBe(0)" },
      { description: 'result is a number', assertion: "expect(typeof Math.hypot(0)).toBe('number')" },
      { description: 'Math.hypot(0) === 0', assertion: "expect(Math.hypot(0) === 0).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.hypot(0))).toBeTruthy()" },
      { description: 'absolute value is less than 0.001', assertion: "expect(Math.abs(Math.hypot(0)) < 0.001).toBeTruthy()" },
    ],
    hints: ['sqrt(0^2) = sqrt(0) = 0'],
    tags: ['Math', 'hypot', 'static-method'],
  },
  {
    slug: 'math-hypot-4',
    title: 'Math.hypot() — Unit Diagonal',
    description: `## Math.hypot()\n\nReturns the square root of the sum of squares of its arguments.\n\n**Challenge:** Compute the diagonal of a unit square using \`Math.hypot(1, 1)\`, which should equal √2.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'Math',
    method: 'hypot',
    initialCode: `// Use Math.hypot()\n`,
    solution: `Math.hypot(1, 1)`,
    tests: [
      { description: 'Math.hypot(1, 1) ≈ Math.SQRT2', assertion: "expect(Math.abs(Math.hypot(1, 1) - Math.SQRT2) < 1e-10).toBeTruthy()" },
      { description: 'Math.hypot(1, 1) > 1.414', assertion: "expect(Math.hypot(1, 1) > 1.414).toBeTruthy()" },
      { description: 'Math.hypot(1, 1) < 1.415', assertion: "expect(Math.hypot(1, 1) < 1.415).toBeTruthy()" },
      { description: 'result is a number', assertion: "expect(typeof Math.hypot(1, 1)).toBe('number')" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.hypot(1, 1))).toBeTruthy()" },
    ],
    hints: ['sqrt(1^2 + 1^2) = sqrt(2) = Math.SQRT2 ≈ 1.41421356...'],
    tags: ['Math', 'hypot', 'static-method'],
  },
  {
    slug: 'math-hypot-5',
    title: 'Math.hypot() — No Arguments',
    description: `## Math.hypot()\n\nReturns the square root of the sum of squares of its arguments.\n\n**Challenge:** Confirm that \`Math.hypot()\` with no arguments returns 0.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'hypot',
    initialCode: `// Use Math.hypot()\n`,
    solution: `Math.hypot()`,
    tests: [
      { description: 'Math.hypot() returns 0', assertion: "expect(Math.hypot()).toBe(0)" },
      { description: 'result is a number', assertion: "expect(typeof Math.hypot()).toBe('number')" },
      { description: 'Math.hypot() === 0', assertion: "expect(Math.hypot() === 0).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.hypot())).toBeTruthy()" },
      { description: 'absolute value is less than 0.001', assertion: "expect(Math.abs(Math.hypot()) < 0.001).toBeTruthy()" },
    ],
    hints: ['sqrt(sum of empty set) = sqrt(0) = 0 by convention'],
    tags: ['Math', 'hypot', 'static-method'],
  },
]
