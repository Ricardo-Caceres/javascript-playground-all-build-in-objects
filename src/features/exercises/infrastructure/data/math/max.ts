import type { Exercise } from '@/shared/types/exercises'

export const mathMaxExercises: Exercise[] = [
  {
    slug: 'math-max-1',
    title: 'Math.max() — Basic Maximum',
    description: `## Math.max()\n\nReturns the largest of the given numbers.\n\n**Challenge:** Use \`Math.max(1, 2, 3)\` to find the largest value.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'max',
    initialCode: `// Use Math.max()\n`,
    solution: `Math.max(1, 2, 3)`,
    tests: [
      { description: 'Math.max(1, 2, 3) returns 3', assertion: "expect(Math.max(1, 2, 3)).toBe(3)" },
      { description: 'result is a number', assertion: "expect(typeof Math.max(1, 2, 3)).toBe('number')" },
      { description: 'Math.max(1, 2, 3) === 3', assertion: "expect(Math.max(1, 2, 3) === 3).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.max(1, 2, 3))).toBeTruthy()" },
      { description: 'result is greater than 2', assertion: "expect(Math.max(1, 2, 3) > 2).toBeTruthy()" },
    ],
    hints: ['Math.max returns the largest of all provided arguments'],
    tags: ['Math', 'max', 'static-method'],
  },
  {
    slug: 'math-max-2',
    title: 'Math.max() — All Negatives',
    description: `## Math.max()\n\nReturns the largest of the given numbers.\n\n**Challenge:** Use \`Math.max(-1, -5, -2)\` to find the maximum among negative numbers.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'max',
    initialCode: `// Use Math.max()\n`,
    solution: `Math.max(-1, -5, -2)`,
    tests: [
      { description: 'Math.max(-1, -5, -2) returns -1', assertion: "expect(Math.max(-1, -5, -2)).toBe(-1)" },
      { description: 'result is a number', assertion: "expect(typeof Math.max(-1, -5, -2)).toBe('number')" },
      { description: 'Math.max(-1, -5, -2) === -1', assertion: "expect(Math.max(-1, -5, -2) === -1).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.max(-1, -5, -2))).toBeTruthy()" },
      { description: 'result is greater than -2', assertion: "expect(Math.max(-1, -5, -2) > -2).toBeTruthy()" },
    ],
    hints: ['-1 is the largest (closest to zero) among -1, -5, -2'],
    tags: ['Math', 'max', 'static-method'],
  },
  {
    slug: 'math-max-3',
    title: 'Math.max() — No Arguments',
    description: `## Math.max()\n\nReturns the largest of the given numbers.\n\n**Challenge:** Observe that \`Math.max()\` with no arguments returns -Infinity.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'Math',
    method: 'max',
    initialCode: `// Use Math.max()\n`,
    solution: `Math.max()`,
    tests: [
      { description: 'Math.max() returns -Infinity', assertion: "expect(Math.max()).toBe(-Infinity)" },
      { description: 'result is not finite', assertion: "expect(!isFinite(Math.max())).toBeTruthy()" },
      { description: 'result is a number type', assertion: "expect(typeof Math.max()).toBe('number')" },
      { description: 'Math.max() === -Infinity', assertion: "expect(Math.max() === -Infinity).toBeTruthy()" },
      { description: 'result is less than 0', assertion: "expect(Math.max() < 0).toBeTruthy()" },
    ],
    hints: ['The identity element for max is -Infinity (every number is ≥ -Infinity)'],
    tags: ['Math', 'max', 'static-method'],
  },
  {
    slug: 'math-max-4',
    title: 'Math.max() — With Infinity',
    description: `## Math.max()\n\nReturns the largest of the given numbers.\n\n**Challenge:** Verify that \`Math.max(Infinity, 1)\` returns Infinity.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'Math',
    method: 'max',
    initialCode: `// Use Math.max()\n`,
    solution: `Math.max(Infinity, 1)`,
    tests: [
      { description: 'Math.max(Infinity, 1) returns Infinity', assertion: "expect(Math.max(Infinity, 1)).toBe(Infinity)" },
      { description: 'result is not finite', assertion: "expect(!isFinite(Math.max(Infinity, 1))).toBeTruthy()" },
      { description: 'result is a number type', assertion: "expect(typeof Math.max(Infinity, 1)).toBe('number')" },
      { description: 'Math.max(Infinity, 1) === Infinity', assertion: "expect(Math.max(Infinity, 1) === Infinity).toBeTruthy()" },
      { description: 'result is greater than 1000', assertion: "expect(Math.max(Infinity, 1) > 1000).toBeTruthy()" },
    ],
    hints: ['Infinity is larger than any finite number'],
    tags: ['Math', 'max', 'static-method'],
  },
  {
    slug: 'math-max-5',
    title: 'Math.max() — With NaN',
    description: `## Math.max()\n\nReturns the largest of the given numbers.\n\n**Challenge:** Observe that \`Math.max(1, NaN)\` returns NaN.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'Math',
    method: 'max',
    initialCode: `// Use Math.max()\n`,
    solution: `Math.max(1, NaN)`,
    tests: [
      { description: 'Math.max(1, NaN) is NaN', assertion: "expect(isNaN(Math.max(1, NaN))).toBeTruthy()" },
      { description: 'result is a number type', assertion: "expect(typeof Math.max(1, NaN)).toBe('number')" },
      { description: 'NaN is not equal to itself', assertion: "expect(Math.max(1, NaN) !== Math.max(1, NaN)).toBeTruthy()" },
      { description: 'Math.max(NaN, NaN) is also NaN', assertion: "expect(isNaN(Math.max(NaN, NaN))).toBeTruthy()" },
      { description: 'Math.max(100, NaN) is also NaN', assertion: "expect(isNaN(Math.max(100, NaN))).toBeTruthy()" },
    ],
    hints: ['Any comparison involving NaN produces NaN'],
    tags: ['Math', 'max', 'static-method'],
  },
]
