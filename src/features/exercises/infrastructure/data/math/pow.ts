import type { Exercise } from '@/shared/types/exercises'

export const mathPowExercises: Exercise[] = [
  {
    slug: 'math-pow-1',
    title: 'Math.pow() — 2^10 = 1024',
    description: `## Math.pow()\n\nReturns the base raised to the exponent power.\n\n**Challenge:** Use \`Math.pow(2, 10)\` to compute 2 to the power of 10.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'pow',
    initialCode: `// Use Math.pow()\n`,
    solution: `Math.pow(2, 10)`,
    tests: [
      { description: 'Math.pow(2, 10) returns 1024', assertion: "expect(Math.pow(2, 10)).toBe(1024)" },
      { description: 'result is a number', assertion: "expect(typeof Math.pow(2, 10)).toBe('number')" },
      { description: 'Math.pow(2, 10) === 1024', assertion: "expect(Math.pow(2, 10) === 1024).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.pow(2, 10))).toBeTruthy()" },
      { description: 'result is greater than 1000', assertion: "expect(Math.pow(2, 10) > 1000).toBeTruthy()" },
    ],
    hints: ['2^10 = 1024, commonly known as 1 kilobyte in computing'],
    tags: ['Math', 'pow', 'static-method'],
  },
  {
    slug: 'math-pow-2',
    title: 'Math.pow() — 3^3 = 27',
    description: `## Math.pow()\n\nReturns the base raised to the exponent power.\n\n**Challenge:** Use \`Math.pow(3, 3)\` to compute 3 cubed.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'pow',
    initialCode: `// Use Math.pow()\n`,
    solution: `Math.pow(3, 3)`,
    tests: [
      { description: 'Math.pow(3, 3) returns 27', assertion: "expect(Math.pow(3, 3)).toBe(27)" },
      { description: 'result is a number', assertion: "expect(typeof Math.pow(3, 3)).toBe('number')" },
      { description: 'Math.pow(3, 3) === 27', assertion: "expect(Math.pow(3, 3) === 27).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.pow(3, 3))).toBeTruthy()" },
      { description: 'difference from 27 is less than 0.001', assertion: "expect(Math.abs(Math.pow(3, 3) - 27) < 0.001).toBeTruthy()" },
    ],
    hints: ['3^3 = 3 × 3 × 3 = 27'],
    tags: ['Math', 'pow', 'static-method'],
  },
  {
    slug: 'math-pow-3',
    title: 'Math.pow() — Square Root via Exponent',
    description: `## Math.pow()\n\nReturns the base raised to the exponent power.\n\n**Challenge:** Use \`Math.pow(4, 0.5)\` to compute the square root of 4.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'Math',
    method: 'pow',
    initialCode: `// Use Math.pow()\n`,
    solution: `Math.pow(4, 0.5)`,
    tests: [
      { description: 'Math.pow(4, 0.5) returns 2', assertion: "expect(Math.pow(4, 0.5)).toBe(2)" },
      { description: 'result is a number', assertion: "expect(typeof Math.pow(4, 0.5)).toBe('number')" },
      { description: 'Math.pow(4, 0.5) === 2', assertion: "expect(Math.pow(4, 0.5) === 2).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.pow(4, 0.5))).toBeTruthy()" },
      { description: 'difference from 2 is less than 0.001', assertion: "expect(Math.abs(Math.pow(4, 0.5) - 2) < 0.001).toBeTruthy()" },
    ],
    hints: ['x^0.5 = sqrt(x); Math.pow(4, 0.5) = Math.sqrt(4) = 2'],
    tags: ['Math', 'pow', 'static-method'],
  },
  {
    slug: 'math-pow-4',
    title: 'Math.pow() — Negative Exponent',
    description: `## Math.pow()\n\nReturns the base raised to the exponent power.\n\n**Challenge:** Use \`Math.pow(2, -1)\` to compute 2 to the power of -1.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'Math',
    method: 'pow',
    initialCode: `// Use Math.pow()\n`,
    solution: `Math.pow(2, -1)`,
    tests: [
      { description: 'Math.pow(2, -1) returns 0.5', assertion: "expect(Math.pow(2, -1)).toBe(0.5)" },
      { description: 'result is a number', assertion: "expect(typeof Math.pow(2, -1)).toBe('number')" },
      { description: 'Math.pow(2, -1) === 0.5', assertion: "expect(Math.pow(2, -1) === 0.5).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.pow(2, -1))).toBeTruthy()" },
      { description: 'difference from 0.5 is less than 0.001', assertion: "expect(Math.abs(Math.pow(2, -1) - 0.5) < 0.001).toBeTruthy()" },
    ],
    hints: ['x^(-1) = 1/x; Math.pow(2, -1) = 1/2 = 0.5'],
    tags: ['Math', 'pow', 'static-method'],
  },
  {
    slug: 'math-pow-5',
    title: 'Math.pow() — Zero to Zero',
    description: `## Math.pow()\n\nReturns the base raised to the exponent power.\n\n**Challenge:** Verify that \`Math.pow(0, 0)\` returns 1 by convention.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'Math',
    method: 'pow',
    initialCode: `// Use Math.pow()\n`,
    solution: `Math.pow(0, 0)`,
    tests: [
      { description: 'Math.pow(0, 0) returns 1', assertion: "expect(Math.pow(0, 0)).toBe(1)" },
      { description: 'result is a number', assertion: "expect(typeof Math.pow(0, 0)).toBe('number')" },
      { description: 'Math.pow(0, 0) === 1', assertion: "expect(Math.pow(0, 0) === 1).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.pow(0, 0))).toBeTruthy()" },
      { description: 'difference from 1 is less than 0.001', assertion: "expect(Math.abs(Math.pow(0, 0) - 1) < 0.001).toBeTruthy()" },
    ],
    hints: ['0^0 = 1 by convention in JavaScript (and most programming languages)'],
    tags: ['Math', 'pow', 'static-method'],
  },
]
