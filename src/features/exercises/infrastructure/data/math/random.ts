import type { Exercise } from '@/shared/types/exercises'

export const mathRandomExercises: Exercise[] = [
  {
    slug: 'math-random-1',
    title: 'Math.random() — Returns a Number',
    description: `## Math.random()\n\nReturns a pseudo-random floating-point number in the range [0, 1).\n\n**Challenge:** Verify that \`Math.random()\` returns a JavaScript number.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'random',
    initialCode: `// Use Math.random()\n`,
    solution: `Math.random()`,
    tests: [
      { description: 'typeof Math.random() is number', assertion: "expect(typeof Math.random()).toBe('number')" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.random())).toBeTruthy()" },
      { description: 'result is >= 0', assertion: "expect(Math.random() >= 0).toBeTruthy()" },
      { description: 'result is < 1', assertion: "expect(Math.random() < 1).toBeTruthy()" },
      { description: 'typeof is number string', assertion: "expect(typeof Math.random() === 'number').toBeTruthy()" },
    ],
    hints: ['Math.random() always returns a number primitive'],
    tags: ['Math', 'random', 'static-method'],
  },
  {
    slug: 'math-random-2',
    title: 'Math.random() — Result >= 0',
    description: `## Math.random()\n\nReturns a pseudo-random floating-point number in the range [0, 1).\n\n**Challenge:** Verify that \`Math.random()\` always returns a value greater than or equal to 0.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'random',
    initialCode: `// Use Math.random()\n`,
    solution: `Math.random() >= 0`,
    tests: [
      { description: 'Math.random() >= 0', assertion: "expect(Math.random() >= 0).toBeTruthy()" },
      { description: 'Math.random() > -1', assertion: "expect(Math.random() > -1).toBeTruthy()" },
      { description: 'Math.random() >= 0 (second check)', assertion: "expect(Math.random() >= 0).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.random())).toBeTruthy()" },
      { description: 'Math.random() >= 0 (third check)', assertion: "expect(Math.random() >= 0).toBeTruthy()" },
    ],
    hints: ['The range of Math.random() is [0, 1) — inclusive of 0, exclusive of 1'],
    tags: ['Math', 'random', 'static-method'],
  },
  {
    slug: 'math-random-3',
    title: 'Math.random() — Result < 1',
    description: `## Math.random()\n\nReturns a pseudo-random floating-point number in the range [0, 1).\n\n**Challenge:** Verify that \`Math.random()\` always returns a value strictly less than 1.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'random',
    initialCode: `// Use Math.random()\n`,
    solution: `Math.random() < 1`,
    tests: [
      { description: 'Math.random() < 1', assertion: "expect(Math.random() < 1).toBeTruthy()" },
      { description: 'Math.random() < 1 (second check)', assertion: "expect(Math.random() < 1).toBeTruthy()" },
      { description: 'Math.random() < 1 (third check)', assertion: "expect(Math.random() < 1).toBeTruthy()" },
      { description: 'Math.random() < 1 (fourth check)', assertion: "expect(Math.random() < 1).toBeTruthy()" },
      { description: 'Math.random() < 1 (fifth check)', assertion: "expect(Math.random() < 1).toBeTruthy()" },
    ],
    hints: ['Math.random() never returns 1; the range is [0, 1)'],
    tags: ['Math', 'random', 'static-method'],
  },
  {
    slug: 'math-random-4',
    title: 'Math.random() — Always a Number Type',
    description: `## Math.random()\n\nReturns a pseudo-random floating-point number in the range [0, 1).\n\n**Challenge:** Confirm that every call to \`Math.random()\` returns a value of type \`'number'\`.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'Math',
    method: 'random',
    initialCode: `// Use Math.random()\n`,
    solution: `typeof Math.random()`,
    tests: [
      { description: 'typeof Math.random() is number', assertion: "expect(typeof Math.random()).toBe('number')" },
      { description: 'typeof Math.random() is number (second check)', assertion: "expect(typeof Math.random()).toBe('number')" },
      { description: 'typeof Math.random() is number (third check)', assertion: "expect(typeof Math.random()).toBe('number')" },
      { description: 'typeof Math.random() is number (fourth check)', assertion: "expect(typeof Math.random()).toBe('number')" },
      { description: 'typeof Math.random() is number (fifth check)', assertion: "expect(typeof Math.random()).toBe('number')" },
    ],
    hints: ['Math.random() consistently returns a number primitive'],
    tags: ['Math', 'random', 'static-method'],
  },
  {
    slug: 'math-random-5',
    title: 'Math.random() — Dice Roll 0-5',
    description: `## Math.random()\n\nReturns a pseudo-random floating-point number in the range [0, 1).\n\n**Challenge:** Use \`Math.floor(Math.random() * 6)\` to simulate a die roll producing integers 0–5.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'Math',
    method: 'random',
    initialCode: `// Use Math.random()\n`,
    solution: `Math.floor(Math.random() * 6)`,
    tests: [
      { description: 'result >= 0', assertion: "expect(Math.floor(Math.random() * 6) >= 0).toBeTruthy()" },
      { description: 'result <= 5', assertion: "expect(Math.floor(Math.random() * 6) <= 5).toBeTruthy()" },
      { description: 'result is a number type', assertion: "expect(typeof Math.floor(Math.random() * 6)).toBe('number')" },
      { description: 'result >= 0 (second check)', assertion: "expect(Math.floor(Math.random() * 6) >= 0).toBeTruthy()" },
      { description: 'result < 6', assertion: "expect(Math.floor(Math.random() * 6) < 6).toBeTruthy()" },
    ],
    hints: ['Math.floor(Math.random() * N) produces integers from 0 to N-1'],
    tags: ['Math', 'random', 'static-method'],
  },
]
