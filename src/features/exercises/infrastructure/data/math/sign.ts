import type { Exercise } from '@/shared/types/exercises'

export const mathSignExercises: Exercise[] = [
  {
    slug: 'math-sign-1',
    title: 'Math.sign() — Positive Number',
    description: `## Math.sign()\n\nReturns the sign of a number, indicating whether it is positive, negative, or zero.\n\n**Challenge:** Verify that \`Math.sign(5)\` returns 1.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'sign',
    initialCode: `// Use Math.sign()\n`,
    solution: `Math.sign(5)`,
    tests: [
      { description: 'Math.sign(5) returns 1', assertion: "expect(Math.sign(5)).toBe(1)" },
      { description: 'result is a number', assertion: "expect(typeof Math.sign(5)).toBe('number')" },
      { description: 'Math.sign(5) === 1', assertion: "expect(Math.sign(5) === 1).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.sign(5))).toBeTruthy()" },
      { description: 'result is positive', assertion: "expect(Math.sign(5) > 0).toBeTruthy()" },
    ],
    hints: ['Math.sign returns 1 for any positive number'],
    tags: ['Math', 'sign', 'static-method'],
  },
  {
    slug: 'math-sign-2',
    title: 'Math.sign() — Negative Number',
    description: `## Math.sign()\n\nReturns the sign of a number, indicating whether it is positive, negative, or zero.\n\n**Challenge:** Verify that \`Math.sign(-3)\` returns -1.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'sign',
    initialCode: `// Use Math.sign()\n`,
    solution: `Math.sign(-3)`,
    tests: [
      { description: 'Math.sign(-3) returns -1', assertion: "expect(Math.sign(-3)).toBe(-1)" },
      { description: 'result is a number', assertion: "expect(typeof Math.sign(-3)).toBe('number')" },
      { description: 'Math.sign(-3) === -1', assertion: "expect(Math.sign(-3) === -1).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.sign(-3))).toBeTruthy()" },
      { description: 'result is negative', assertion: "expect(Math.sign(-3) < 0).toBeTruthy()" },
    ],
    hints: ['Math.sign returns -1 for any negative number'],
    tags: ['Math', 'sign', 'static-method'],
  },
  {
    slug: 'math-sign-3',
    title: 'Math.sign() — Zero',
    description: `## Math.sign()\n\nReturns the sign of a number, indicating whether it is positive, negative, or zero.\n\n**Challenge:** Verify that \`Math.sign(0)\` returns 0.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'sign',
    initialCode: `// Use Math.sign()\n`,
    solution: `Math.sign(0)`,
    tests: [
      { description: 'Math.sign(0) returns 0', assertion: "expect(Math.sign(0)).toBe(0)" },
      { description: 'result is a number', assertion: "expect(typeof Math.sign(0)).toBe('number')" },
      { description: 'Math.sign(0) === 0', assertion: "expect(Math.sign(0) === 0).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.sign(0))).toBeTruthy()" },
      { description: 'absolute value is less than 0.001', assertion: "expect(Math.abs(Math.sign(0)) < 0.001).toBeTruthy()" },
    ],
    hints: ['Math.sign(0) returns +0, which equals 0 with ==='],
    tags: ['Math', 'sign', 'static-method'],
  },
  {
    slug: 'math-sign-4',
    title: 'Math.sign() — Negative Zero',
    description: `## Math.sign()\n\nReturns the sign of a number, indicating whether it is positive, negative, or zero.\n\n**Challenge:** Explore \`Math.sign(-0)\` which returns -0, distinguishable via \`Object.is\`.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'Math',
    method: 'sign',
    initialCode: `// Use Math.sign()\n`,
    solution: `Math.sign(-0)`,
    tests: [
      { description: 'Object.is(Math.sign(-0), -0) is true', assertion: "expect(Object.is(Math.sign(-0), -0)).toBeTruthy()" },
      { description: 'result is a number', assertion: "expect(typeof Math.sign(-0)).toBe('number')" },
      { description: 'Math.sign(-0) === 0 (== treats -0 as 0)', assertion: "expect(Math.sign(-0) === 0).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.sign(-0))).toBeTruthy()" },
      { description: 'result is <= 0', assertion: "expect(Math.sign(-0) <= 0).toBeTruthy()" },
    ],
    hints: ['JavaScript has -0 as a distinct value; use Object.is to differentiate it from +0'],
    tags: ['Math', 'sign', 'static-method'],
  },
  {
    slug: 'math-sign-5',
    title: 'Math.sign() — NaN Input',
    description: `## Math.sign()\n\nReturns the sign of a number, indicating whether it is positive, negative, or zero.\n\n**Challenge:** Observe that \`Math.sign(NaN)\` returns NaN.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'Math',
    method: 'sign',
    initialCode: `// Use Math.sign()\n`,
    solution: `Math.sign(NaN)`,
    tests: [
      { description: 'Math.sign(NaN) is NaN', assertion: "expect(isNaN(Math.sign(NaN))).toBeTruthy()" },
      { description: 'result is a number type', assertion: "expect(typeof Math.sign(NaN)).toBe('number')" },
      { description: 'NaN is not equal to itself', assertion: "expect(Math.sign(NaN) !== Math.sign(NaN)).toBeTruthy()" },
      { description: 'isNaN is true', assertion: "expect(isNaN(Math.sign(NaN))).toBeTruthy()" },
      { description: 'typeof is number', assertion: "expect(typeof Math.sign(NaN)).toBe('number')" },
    ],
    hints: ['NaN propagates through Math.sign — NaN has no defined sign'],
    tags: ['Math', 'sign', 'static-method'],
  },
]
