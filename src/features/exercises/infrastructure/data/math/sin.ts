import type { Exercise } from '@/shared/types/exercises'

export const mathSinExercises: Exercise[] = [
  {
    slug: 'math-sin-1',
    title: 'Math.sin() — sin(0) = 0',
    description: `## Math.sin()\n\nReturns the sine of a number (in radians).\n\n**Challenge:** Verify that \`Math.sin(0)\` returns 0.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'sin',
    initialCode: `// Use Math.sin()\n`,
    solution: `Math.sin(0)`,
    tests: [
      { description: 'Math.sin(0) returns 0', assertion: "expect(Math.sin(0)).toBe(0)" },
      { description: 'result is a number', assertion: "expect(typeof Math.sin(0)).toBe('number')" },
      { description: 'Math.sin(0) === 0', assertion: "expect(Math.sin(0) === 0).toBeTruthy()" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.sin(0))).toBeTruthy()" },
      { description: 'absolute value is less than 0.001', assertion: "expect(Math.abs(Math.sin(0)) < 0.001).toBeTruthy()" },
    ],
    hints: ['sin(0) = 0 — the sine of 0 radians is exactly 0'],
    tags: ['Math', 'sin', 'static-method'],
  },
  {
    slug: 'math-sin-2',
    title: 'Math.sin() — sin(π/2) = 1',
    description: `## Math.sin()\n\nReturns the sine of a number (in radians).\n\n**Challenge:** Verify that \`Math.sin(Math.PI/2)\` returns approximately 1.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'Math',
    method: 'sin',
    initialCode: `// Use Math.sin()\n`,
    solution: `Math.sin(Math.PI / 2)`,
    tests: [
      { description: 'Math.sin(Math.PI/2) ≈ 1', assertion: "expect(Math.abs(Math.sin(Math.PI/2) - 1) < 1e-10).toBeTruthy()" },
      { description: 'Math.sin(Math.PI/2) > 0.999', assertion: "expect(Math.sin(Math.PI/2) > 0.999).toBeTruthy()" },
      { description: 'Math.sin(Math.PI/2) < 1.001', assertion: "expect(Math.sin(Math.PI/2) < 1.001).toBeTruthy()" },
      { description: 'result is a number', assertion: "expect(typeof Math.sin(Math.PI/2)).toBe('number')" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.sin(Math.PI/2))).toBeTruthy()" },
    ],
    hints: ['sin(π/2) = 1 — the maximum value of the sine function'],
    tags: ['Math', 'sin', 'static-method'],
  },
  {
    slug: 'math-sin-3',
    title: 'Math.sin() — sin(π) ≈ 0',
    description: `## Math.sin()\n\nReturns the sine of a number (in radians).\n\n**Challenge:** Observe that \`Math.sin(Math.PI)\` returns a value very close to 0 (floating-point approximation).`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'Math',
    method: 'sin',
    initialCode: `// Use Math.sin()\n`,
    solution: `Math.sin(Math.PI)`,
    tests: [
      { description: 'Math.sin(Math.PI) ≈ 0', assertion: "expect(Math.abs(Math.sin(Math.PI)) < 1e-10).toBeTruthy()" },
      { description: 'result is a number', assertion: "expect(typeof Math.sin(Math.PI)).toBe('number')" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.sin(Math.PI))).toBeTruthy()" },
      { description: 'result > -0.001', assertion: "expect(Math.sin(Math.PI) > -0.001).toBeTruthy()" },
      { description: 'result < 0.001', assertion: "expect(Math.sin(Math.PI) < 0.001).toBeTruthy()" },
    ],
    hints: ['sin(π) = 0 mathematically, but floating-point gives a tiny near-zero value'],
    tags: ['Math', 'sin', 'static-method'],
  },
  {
    slug: 'math-sin-4',
    title: 'Math.sin() — sin(-π/2) = -1',
    description: `## Math.sin()\n\nReturns the sine of a number (in radians).\n\n**Challenge:** Verify that \`Math.sin(-Math.PI/2)\` returns approximately -1.`,
    category: 'static-method',
    difficulty: 'intermediate',
    builtIn: 'Math',
    method: 'sin',
    initialCode: `// Use Math.sin()\n`,
    solution: `Math.sin(-Math.PI / 2)`,
    tests: [
      { description: 'Math.sin(-Math.PI/2) ≈ -1', assertion: "expect(Math.abs(Math.sin(-Math.PI/2) - (-1)) < 1e-10).toBeTruthy()" },
      { description: 'Math.sin(-Math.PI/2) < -0.999', assertion: "expect(Math.sin(-Math.PI/2) < -0.999).toBeTruthy()" },
      { description: 'Math.sin(-Math.PI/2) > -1.001', assertion: "expect(Math.sin(-Math.PI/2) > -1.001).toBeTruthy()" },
      { description: 'result is a number', assertion: "expect(typeof Math.sin(-Math.PI/2)).toBe('number')" },
      { description: 'result is finite', assertion: "expect(Number.isFinite(Math.sin(-Math.PI/2))).toBeTruthy()" },
    ],
    hints: ['sin(-π/2) = -1 — the minimum value of the sine function'],
    tags: ['Math', 'sin', 'static-method'],
  },
  {
    slug: 'math-sin-5',
    title: 'Math.sin() — Return Type',
    description: `## Math.sin()\n\nReturns the sine of a number (in radians).\n\n**Challenge:** Confirm that \`Math.sin()\` always returns a JavaScript \`number\`.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Math',
    method: 'sin',
    initialCode: `// Use Math.sin()\n`,
    solution: `typeof Math.sin(0)`,
    tests: [
      { description: 'typeof Math.sin(0) is number', assertion: "expect(typeof Math.sin(0)).toBe('number')" },
      { description: 'typeof Math.sin(1) is number', assertion: "expect(typeof Math.sin(1)).toBe('number')" },
      { description: 'typeof Math.sin(Math.PI) is number', assertion: "expect(typeof Math.sin(Math.PI)).toBe('number')" },
      { description: 'typeof Math.sin(-1) is number', assertion: "expect(typeof Math.sin(-1)).toBe('number')" },
      { description: 'typeof Math.sin(100) is number', assertion: "expect(typeof Math.sin(100)).toBe('number')" },
    ],
    hints: ['Math.sin always returns a number primitive'],
    tags: ['Math', 'sin', 'static-method'],
  },
]
