import type { Exercise } from '@/shared/types/exercises'

export const promiseRaceExercises: Exercise[] = [
  {
    slug: 'promise-race-1',
    title: 'Promise.race() — instanceof',
    description: `## Promise.race()\n\n\`Promise.race([Promise.resolve(1)]) instanceof Promise\` is \`true\`.\n\n**Challenge:** Verify Promise.race returns a Promise.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Promise',
    method: 'race',
    initialCode: `Promise.race([Promise.resolve(1)]) instanceof Promise\n`,
    solution: `Promise.race([Promise.resolve(1)]) instanceof Promise`,
    tests: [
      { description: 'instanceof Promise is true', assertion:'expect(result).toBe(true)' },
      { description: 'instanceof Object is true', assertion:'expect(Promise.race([Promise.resolve(1)]) instanceof Object).toBe(true)' },
      { description: 'is truthy', assertion:'expect(Promise.race([Promise.resolve(1)])).toBeTruthy()' },
      { description: 'is not null', assertion:'expect(Promise.race([Promise.resolve(1)]) !== null).toBe(true)' },
      { description: 'is not undefined', assertion:'expect(Promise.race([Promise.resolve(1)]) !== undefined).toBe(true)' },
    ],
    hints: ['Promise.race() always returns a Promise.'],
    tags: ['promise', 'race', 'instanceof'],
  },
  {
    slug: 'promise-race-2',
    title: 'Promise.race([]) — typeof',
    description: `## typeof Promise.race()\n\n\`typeof Promise.race([])\` is \`'object'\`.\n\n**Challenge:** Verify the return type.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Promise',
    method: 'race',
    initialCode: `typeof Promise.race([])\n`,
    solution: `typeof Promise.race([])`,
    tests: [
      { description: "typeof is 'object'", assertion:"expect(result).toBe('object')" },
      { description: "not 'function'", assertion:"expect(result === 'function').toBe(false)" },
      { description: "not 'string'", assertion:"expect(result === 'string').toBe(false)" },
      { description: 'instanceof Promise', assertion:'expect(Promise.race([]) instanceof Promise).toBe(true)' },
      { description: 'result is truthy', assertion:'expect(Promise.race([])).toBeTruthy()' },
    ],
    hints: ['Promise.race() returns a Promise object.'],
    tags: ['promise', 'race', 'typeof'],
  },
  {
    slug: 'promise-race-3',
    title: 'Promise.race — typeof function',
    description: `## typeof Promise.race\n\n\`typeof Promise.race\` is \`'function'\`.\n\n**Challenge:** Verify Promise.race is a function.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Promise',
    method: 'race',
    initialCode: `typeof Promise.race\n`,
    solution: `typeof Promise.race`,
    tests: [
      { description: "result is 'function'", assertion:"expect(result).toBe('function')" },
      { description: 'result', assertion:'expect(result).toBe(true)' },
      { description: 'Promise.race is truthy', assertion:'expect(Promise.race).toBeTruthy()' },
      { description: 'Promise.race is not null', assertion:'expect(Promise.race !== null).toBe(true)' },
      { description: "'race' in Promise", assertion:"expect('race' in Promise).toBe(true)" },
    ],
    hints: ['Promise.race is a static method (function).'],
    tags: ['promise', 'race', 'typeof'],
  },
  {
    slug: 'promise-race-4',
    title: 'Promise.race() — multiple inputs',
    description: `## Promise.race() Multiple\n\n\`Promise.race([Promise.resolve(1), Promise.resolve(2)]) instanceof Promise\` is \`true\`.\n\n**Challenge:** Verify race with multiple inputs.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Promise',
    method: 'race',
    initialCode: `Promise.race([Promise.resolve(1), Promise.resolve(2)]) instanceof Promise\n`,
    solution: `Promise.race([Promise.resolve(1), Promise.resolve(2)]) instanceof Promise`,
    tests: [
      { description: 'instanceof Promise is true', assertion:'expect(result).toBe(true)' },
      { description: 'typeof is object', assertion:"expect(result([Promise.resolve(1), Promise.resolve(2)])).toBe('object')" },
      { description: 'is truthy', assertion:'expect(Promise.race([Promise.resolve(1), Promise.resolve(2)])).toBeTruthy()' },
      { description: 'three inputs instanceof Promise', assertion:'expect(Promise.race([Promise.resolve(1), Promise.resolve(2), Promise.resolve(3)]) instanceof Promise).toBe(true)' },
      { description: 'is not null', assertion:'expect(Promise.race([Promise.resolve(1), Promise.resolve(2)]) !== null).toBe(true)' },
    ],
    hints: ['Promise.race returns a Promise regardless of input count.'],
    tags: ['promise', 'race', 'multiple'],
  },
  {
    slug: 'promise-race-5',
    title: 'Promise.race — instanceof Function',
    description: `## Promise.race instanceof Function\n\n\`Promise.race instanceof Function\` is \`true\`.\n\n**Challenge:** Verify Promise.race is a Function instance.`,
    category: 'static-method',
    difficulty: 'beginner',
    builtIn: 'Promise',
    method: 'race',
    initialCode: `Promise.race instanceof Function\n`,
    solution: `Promise.race instanceof Function`,
    tests: [
      { description: 'instanceof Function is true', assertion:'expect(result).toBe(true)' },
      { description: 'instanceof Object is true', assertion:'expect(Promise.race instanceof Object).toBe(true)' },
      { description: 'is truthy', assertion:'expect(Promise.race).toBeTruthy()' },
      { description: "typeof is 'function'", assertion:"expect(result).toBe('function')" },
      { description: 'is not null', assertion:'expect(Promise.race !== null).toBe(true)' },
    ],
    hints: ['Static methods are Function instances.'],
    tags: ['promise', 'race', 'instanceof'],
  },
]
