import type { Exercise } from '@/shared/types/exercises'

export const regexpIgnoreCaseExercises: Exercise[] = [
  {
    slug: 'regexp-ignorecase-1',
    title: 'RegExp ignoreCase — i flag set',
    description: `## RegExp.prototype.ignoreCase\n\nThe \`ignoreCase\` property is true when the 'i' flag is set.\n\n**Challenge:** Verify that \`/hi/i.ignoreCase\` equals true.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'RegExp',
    method: 'ignoreCase',
    initialCode: `// Use /hi/i.ignoreCase\n`,
    solution: `/hi/i.ignoreCase`,
    tests: [
      { description: 'result equals true', assertion:'expect(result).toBe(true)' },
      { description: 'ignoreCase is boolean', assertion:"expect(typeof result).toBe('boolean')" },
      { description: 'result is truthy', assertion:'expect(result).toBeTruthy()' },
      { description: "new RegExp('hi','i').ignoreCase is true", assertion:"expect(new RegExp('hi','i').ignoreCase).toBe(true)" },
      { description: "/hi/i.flags includes 'i'", assertion:"expect(/hi/i.flags.includes('i')).toBe(true)" },
    ],
    hints: ['The i flag enables case-insensitive matching'],
    tags: ['RegExp', 'ignoreCase', 'property'],
  },
  {
    slug: 'regexp-ignorecase-2',
    title: 'RegExp ignoreCase — no i flag',
    description: `## RegExp.prototype.ignoreCase\n\nThe \`ignoreCase\` property is false when the 'i' flag is not set.\n\n**Challenge:** Verify that \`/hi/.ignoreCase\` equals false.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'RegExp',
    method: 'ignoreCase',
    initialCode: `// Use /hi/.ignoreCase\n`,
    solution: `/hi/.ignoreCase`,
    tests: [
      { description: 'result equals false', assertion:'expect(result).toBe(false)' },
      { description: 'ignoreCase is boolean', assertion:"expect(typeof result).toBe('boolean')" },
      { description: 'result is falsy', assertion:'expect(result).toBeFalsy()' },
      { description: "new RegExp('hi').ignoreCase is false", assertion:"expect(new RegExp('hi').ignoreCase).toBe(false)" },
      { description: "/hi/.flags does not include 'i'", assertion:"expect(/hi/.flags.includes('i')).toBe(false)" },
    ],
    hints: ['Without the i flag, case-sensitive matching is used by default'],
    tags: ['RegExp', 'ignoreCase', 'property'],
  },
  {
    slug: 'regexp-ignorecase-3',
    title: 'RegExp ignoreCase — gi flags',
    description: `## RegExp.prototype.ignoreCase\n\nWith both g and i flags, \`ignoreCase\` is true.\n\n**Challenge:** Verify that \`/hi/gi.ignoreCase\` equals true.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'RegExp',
    method: 'ignoreCase',
    initialCode: `// Use /hi/gi.ignoreCase\n`,
    solution: `/hi/gi.ignoreCase`,
    tests: [
      { description: 'result equals true', assertion:'expect(result).toBe(true)' },
      { description: '/hi/gi.global equals true', assertion:'expect(/hi/gi.global).toBe(true)' },
      { description: 'ignoreCase is boolean', assertion:"expect(typeof result).toBe('boolean')" },
      { description: "/hi/gi.flags includes 'i'", assertion:"expect(/hi/gi.flags.includes('i')).toBe(true)" },
      { description: '/hi/g.ignoreCase is false', assertion:'expect(/hi/g.ignoreCase).toBe(false)' },
    ],
    hints: ['ignoreCase checks only for the i flag'],
    tags: ['RegExp', 'ignoreCase', 'flags'],
  },
  {
    slug: 'regexp-ignorecase-4',
    title: 'RegExp ignoreCase — typeof',
    description: `## RegExp.prototype.ignoreCase\n\nThe \`ignoreCase\` property always returns a boolean.\n\n**Challenge:** Verify that \`typeof /hi/i.ignoreCase\` is 'boolean'.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'RegExp',
    method: 'ignoreCase',
    initialCode: `// Use typeof /hi/i.ignoreCase\n`,
    solution: `typeof /hi/i.ignoreCase`,
    tests: [
      { description: "typeof result is 'boolean'", assertion:"expect(typeof result).toBe('boolean')" },
      { description: "typeof result is 'boolean'", assertion:"expect(typeof result).toBe('boolean')" },
      { description: 'result is not null', assertion:'expect(result !== null).toBe(true)' },
      { description: 'result equals true', assertion:'expect(result).toBe(true)' },
      { description: 'result equals false', assertion:'expect(result).toBe(false)' },
    ],
    hints: ['ignoreCase is always a boolean value'],
    tags: ['RegExp', 'ignoreCase', 'typeof'],
  },
  {
    slug: 'regexp-ignorecase-5',
    title: 'RegExp ignoreCase — result is boolean',
    description: `## RegExp.prototype.ignoreCase\n\nConfirm that \`ignoreCase\` is strictly a boolean.\n\n**Challenge:** Verify that \`/pattern/i.ignoreCase === true\`.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'RegExp',
    method: 'ignoreCase',
    initialCode: `// Check /pattern/i.ignoreCase === true\n`,
    solution: `/pattern/i.ignoreCase === true`,
    tests: [
      { description: 'result', assertion:'expect(result).toBe(true)' },
      { description: '/pattern/.ignoreCase === false', assertion:'expect(/pattern/.ignoreCase === false).toBe(true)' },
      { description: 'ignoreCase is strictly boolean', assertion:"expect(typeof /pattern/i.ignoreCase).toBe('boolean')" },
      { description: "new RegExp('pat','i').ignoreCase is true", assertion:"expect(new RegExp('pat','i').ignoreCase).toBe(true)" },
      { description: "new RegExp('pat').ignoreCase is false", assertion:"expect(new RegExp('pat').ignoreCase).toBe(false)" },
    ],
    hints: ['ignoreCase returns a strict boolean, not just a truthy/falsy value'],
    tags: ['RegExp', 'ignoreCase', 'boolean'],
  },
]
