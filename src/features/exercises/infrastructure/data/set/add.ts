import type { Exercise } from '@/shared/types/exercises'

export const setAddExercises: Exercise[] = [
  {
    slug: 'set-add-1',
    title: 'Set add() — returns same Set',
    description: `## Set.prototype.add()\n\n\`add()\` adds a value and returns the Set itself for chaining.\n\n**Challenge:** Verify that add() returns the same Set instance.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Set',
    method: 'add',
    initialCode: `// Check that add() returns the Set\nconst s = new Set()\n`,
    solution: `const s = new Set(); s.add(1) === s`,
    tests: [
      { description: 'add() returns the same Set', assertion: 'const s = new Set(); expect(s.add(1)).toBe(s)' },
      { description: 'add() is chainable', assertion: 'const s = new Set(); s.add(1).add(2); expect(s.size).toBe(2)' },
      { description: 'returned set has the new value', assertion: 'const s = new Set(); expect(s.add(1).has(1)).toBe(true)' },
      { description: 'add returns Set instance', assertion: 'const s = new Set(); expect(s.add(1) instanceof Set).toBe(true)' },
      { description: 'chaining adds multiple values', assertion: 'const s = new Set(); s.add(1).add(2).add(3); expect(s.size).toBe(3)' },
    ],
    hints: ['add() returns the Set itself, enabling method chaining'],
    tags: ['Set', 'add', 'chaining'],
  },
  {
    slug: 'set-add-2',
    title: 'Set add() — size increases',
    description: `## Set.prototype.add()\n\nAdding a new value increases size by 1.\n\n**Challenge:** Verify that size increases after add().`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Set',
    method: 'add',
    initialCode: `// Check size after add()\nconst s = new Set()\ns.add('hello')\n`,
    solution: `const s = new Set(); s.add('hello'); s.size`,
    tests: [
      { description: 'size is 1 after one add', assertion: "const s = new Set(); s.add('hello'); expect(s.size).toBe(1)" },
      { description: 'size is 2 after two adds', assertion: "const s = new Set(); s.add('a'); s.add('b'); expect(s.size).toBe(2)" },
      { description: 'has() is true after add', assertion: "const s = new Set(); s.add('x'); expect(s.has('x')).toBe(true)" },
      { description: 'add number increases size', assertion: 'const s = new Set(); s.add(42); expect(s.size).toBe(1)' },
      { description: 'size before add is 0', assertion: 'expect(new Set().size).toBe(0)' },
    ],
    hints: ['Adding a new unique value always increases size'],
    tags: ['Set', 'add', 'size'],
  },
  {
    slug: 'set-add-3',
    title: 'Set add() — duplicate no size increase',
    description: `## Set.prototype.add()\n\nAdding a value that already exists does not increase size.\n\n**Challenge:** Verify that adding a duplicate is a no-op for size.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Set',
    method: 'add',
    initialCode: `// Add a duplicate\nconst s = new Set([1])\ns.add(1)\n`,
    solution: `const s = new Set([1]); s.add(1); s.size`,
    tests: [
      { description: 'size stays 1 after duplicate add', assertion: 'const s = new Set([1]); s.add(1); expect(s.size).toBe(1)' },
      { description: 'multiple duplicate adds still size 1', assertion: 'const s = new Set(); s.add(5); s.add(5); s.add(5); expect(s.size).toBe(1)' },
      { description: 'add still returns Set on duplicate', assertion: 'const s = new Set([1]); expect(s.add(1)).toBe(s)' },
      { description: 'has() still true after duplicate add', assertion: 'const s = new Set([1]); s.add(1); expect(s.has(1)).toBe(true)' },
      { description: 'string dedup', assertion: "const s = new Set(['a']); s.add('a'); expect(s.size).toBe(1)" },
    ],
    hints: ['Sets are unique — adding an existing value does nothing to size'],
    tags: ['Set', 'add', 'deduplication'],
  },
  {
    slug: 'set-add-4',
    title: 'Set add() — chaining',
    description: `## Set.prototype.add()\n\nSince add() returns the Set, you can chain multiple add() calls.\n\n**Challenge:** Verify that chaining add() calls works correctly.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Set',
    method: 'add',
    initialCode: `// Chain add() calls\nconst s = new Set()\ns.add(1).add(2).add(3)\n`,
    solution: `const s = new Set(); s.add(1).add(2).add(3); s.size`,
    tests: [
      { description: 'size is 3 after chaining 3 adds', assertion: 'const s = new Set(); s.add(1).add(2).add(3); expect(s.size).toBe(3)' },
      { description: 'has all chained values', assertion: 'const s = new Set(); s.add(1).add(2).add(3); expect(s.has(1) && s.has(2) && s.has(3)).toBe(true)' },
      { description: 'chaining returns Set', assertion: 'const s = new Set(); expect(s.add(1).add(2) instanceof Set).toBe(true)' },
      { description: 'chained result is same reference', assertion: 'const s = new Set(); expect(s.add(1).add(2)).toBe(s)' },
      { description: 'chaining deduplicates too', assertion: 'const s = new Set(); s.add(1).add(1).add(2); expect(s.size).toBe(2)' },
    ],
    hints: ['add() returns the Set, so you can chain: set.add(1).add(2).add(3)'],
    tags: ['Set', 'add', 'chaining'],
  },
  {
    slug: 'set-add-5',
    title: 'Set add() — add object',
    description: `## Set.prototype.add()\n\nObjects are stored by reference in Sets.\n\n**Challenge:** Verify that an object can be added to a Set.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Set',
    method: 'add',
    initialCode: `// Add an object to a Set\nconst obj = { id: 1 }\nconst s = new Set()\ns.add(obj)\n`,
    solution: `const obj = { id: 1 }; const s = new Set(); s.add(obj); s.has(obj)`,
    tests: [
      { description: 'has(obj) is true after add', assertion: 'const obj = {id:1}; const s = new Set(); s.add(obj); expect(s.has(obj)).toBe(true)' },
      { description: 'different object with same structure is not equal', assertion: 'const s = new Set(); s.add({x:1}); expect(s.has({x:1})).toBe(false)' },
      { description: 'size is 1 after adding object', assertion: 'const s = new Set(); s.add({a:1}); expect(s.size).toBe(1)' },
      { description: 'same object added twice is size 1', assertion: 'const obj = {}; const s = new Set(); s.add(obj); s.add(obj); expect(s.size).toBe(1)' },
      { description: 'two different objects are both added', assertion: 'const s = new Set(); s.add({}); s.add({}); expect(s.size).toBe(2)' },
    ],
    hints: ['Objects are compared by reference in Sets, not by structural equality'],
    tags: ['Set', 'add', 'object'],
  },
]
