import type { Exercise } from '@/shared/types/exercises'

export const setDifferenceExercises: Exercise[] = [
  {
    slug: 'set-difference-1',
    title: 'Set difference() — elements in A not B',
    description: `## Set.prototype.difference() (ES2025)\n\n\`setA.difference(setB)\` returns a new Set with elements in A but not in B.\n\n**Challenge:** Verify the difference of two Sets.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Set',
    method: 'difference',
    initialCode: `// Use difference() - ES2025\nconst a = new Set([1,2,3,4])\nconst b = new Set([3,4,5])\n`,
    solution: `[...(new Set([1,2,3,4]) as any).difference(new Set([3,4,5]))]`,
    tests: [
      { description: 'difference has elements from A not in B', assertion: 'expect([...(new Set([1,2,3,4]) as any).difference(new Set([3,4,5]))].sort((a,b)=>a-b)).toEqual([1,2])' },
      { description: 'has 1', assertion: 'expect((new Set([1,2,3,4]) as any).difference(new Set([3,4,5])).has(1)).toBe(true)' },
      { description: 'has 2', assertion: 'expect((new Set([1,2,3,4]) as any).difference(new Set([3,4,5])).has(2)).toBe(true)' },
      { description: 'does not have 3', assertion: 'expect((new Set([1,2,3,4]) as any).difference(new Set([3,4,5])).has(3)).toBe(false)' },
      { description: 'result is a Set', assertion: 'expect((new Set([1,2,3,4]) as any).difference(new Set([3,4,5])) instanceof Set).toBe(true)' },
    ],
    hints: ['A.difference(B) = A - B — elements in A that are not in B'],
    tags: ['Set', 'difference', 'ES2025'],
  },
  {
    slug: 'set-difference-2',
    title: 'Set difference() — size',
    description: `## Set.prototype.difference() (ES2025)\n\nVerify the size of the difference Set.\n\n**Challenge:** Verify size of A.difference(B).`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Set',
    method: 'difference',
    initialCode: `// Check difference size\nconst a = new Set([1,2,3,4])\nconst b = new Set([3,4])\n`,
    solution: `(new Set([1,2,3,4]) as any).difference(new Set([3,4])).size`,
    tests: [
      { description: 'difference size is 2', assertion: 'expect((new Set([1,2,3,4]) as any).difference(new Set([3,4])).size).toBe(2)' },
      { description: 'difference with empty is same size', assertion: 'expect((new Set([1,2,3]) as any).difference(new Set()).size).toBe(3)' },
      { description: 'B superset of A: difference is empty', assertion: 'expect((new Set([1,2]) as any).difference(new Set([1,2,3,4])).size).toBe(0)' },
      { description: 'size is a number', assertion: "expect(typeof (new Set([1,2]) as any).difference(new Set([2,3])).size).toBe('number')" },
      { description: 'complete overlap: size is 0', assertion: 'expect((new Set([1,2,3]) as any).difference(new Set([1,2,3])).size).toBe(0)' },
    ],
    hints: ['difference size = size of A minus the count of elements in both A and B'],
    tags: ['Set', 'difference', 'size'],
  },
  {
    slug: 'set-difference-3',
    title: 'Set difference() — empty result',
    description: `## Set.prototype.difference() (ES2025)\n\nWhen A is a subset of B, difference is empty.\n\n**Challenge:** Verify that difference is empty when A ⊆ B.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Set',
    method: 'difference',
    initialCode: `// A is subset of B\nconst a = new Set([1,2])\nconst b = new Set([1,2,3,4])\n`,
    solution: `(new Set([1,2]) as any).difference(new Set([1,2,3,4])).size`,
    tests: [
      { description: 'A subset of B: difference size is 0', assertion: 'expect((new Set([1,2]) as any).difference(new Set([1,2,3,4])).size).toBe(0)' },
      { description: 'identical sets: difference is empty', assertion: 'expect((new Set([1,2,3]) as any).difference(new Set([1,2,3])).size).toBe(0)' },
      { description: 'empty result is empty Set', assertion: 'expect([...(new Set([1,2]) as any).difference(new Set([1,2,3]))]).toEqual([])' },
      { description: 'result is a Set', assertion: 'expect((new Set([1]) as any).difference(new Set([1,2])) instanceof Set).toBe(true)' },
      { description: 'empty A: difference is empty', assertion: 'expect((new Set() as any).difference(new Set([1,2])).size).toBe(0)' },
    ],
    hints: ['When all elements of A are in B, the difference is an empty Set'],
    tags: ['Set', 'difference', 'empty'],
  },
  {
    slug: 'set-difference-4',
    title: 'Set difference() — B empty keeps A',
    description: `## Set.prototype.difference() (ES2025)\n\nWhen B is empty, difference equals A.\n\n**Challenge:** Verify that A.difference(empty) equals A.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Set',
    method: 'difference',
    initialCode: `// B is empty\nconst a = new Set([1,2,3])\nconst b = new Set()\n`,
    solution: `(new Set([1,2,3]) as any).difference(new Set()).size`,
    tests: [
      { description: 'difference with empty B has same size as A', assertion: 'expect((new Set([1,2,3]) as any).difference(new Set()).size).toBe(3)' },
      { description: 'all A elements in result', assertion: 'const r = (new Set([1,2,3]) as any).difference(new Set()); expect(r.has(1) && r.has(2) && r.has(3)).toBe(true)' },
      { description: 'result is a new Set', assertion: 'const a = new Set([1,2,3]); const r = (a as any).difference(new Set()); expect(r).not.toBe(a)' },
      { description: 'B empty → result equals A', assertion: 'expect([...(new Set([4,5,6]) as any).difference(new Set())].sort((a,b)=>a-b)).toEqual([4,5,6])' },
      { description: 'result is Set instance', assertion: 'expect((new Set([1,2]) as any).difference(new Set()) instanceof Set).toBe(true)' },
    ],
    hints: ['A - ∅ = A; removing nothing leaves everything'],
    tags: ['Set', 'difference', 'emptyB'],
  },
  {
    slug: 'set-difference-5',
    title: 'Set difference() — originals unchanged',
    description: `## Set.prototype.difference() (ES2025)\n\ndifference() does not modify the original Sets.\n\n**Challenge:** Verify originals are unchanged after difference().`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Set',
    method: 'difference',
    initialCode: `// Check originals unchanged\nconst a = new Set([1,2,3])\nconst b = new Set([2,3,4])\n`,
    solution: `const a = new Set([1,2,3]); const b = new Set([2,3,4]); (a as any).difference(b); a.size`,
    tests: [
      { description: 'original a unchanged', assertion: 'const a = new Set([1,2,3]); (a as any).difference(new Set([2,3])); expect(a.size).toBe(3)' },
      { description: 'original b unchanged', assertion: 'const a = new Set([1,2,3]); const b = new Set([2,3,4]); (a as any).difference(b); expect(b.size).toBe(3)' },
      { description: 'result is a new Set', assertion: 'const a = new Set([1,2,3]); const r = (a as any).difference(new Set([2])); expect(r).not.toBe(a)' },
      { description: 'a still has all original elements', assertion: 'const a = new Set([1,2,3]); (a as any).difference(new Set([1])); expect(a.has(1)).toBe(true)' },
      { description: 'result contains only A-exclusive elements', assertion: 'const r = (new Set([1,2,3]) as any).difference(new Set([2,3,4])); expect(r.has(1) && !r.has(4)).toBe(true)' },
    ],
    hints: ['difference() returns a new Set; neither original is modified'],
    tags: ['Set', 'difference', 'immutable'],
  },
]
