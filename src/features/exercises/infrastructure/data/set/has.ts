import type { Exercise } from '@/shared/types/exercises'

export const setHasExercises: Exercise[] = [
  {
    slug: 'set-has-1',
    title: 'Set has() — existing value',
    description: `## Set.prototype.has()\n\n\`has(value)\` returns true if the value is in the Set.\n\n**Challenge:** Verify that has() returns true for an existing value.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Set',
    method: 'has',
    initialCode: `// Use has() to check for a value\nconst s = new Set([1, 2, 3])\n`,
    solution: `new Set([1,2,3]).has(2)`,
    tests: [
      { description: 'has(2) returns true', assertion: 'expect(new Set([1,2,3]).has(2)).toBe(true)' },
      { description: 'result is boolean', assertion: "expect(typeof new Set([1,2,3]).has(2)).toBe('boolean')" },
      { description: 'result is truthy', assertion: 'expect(new Set([1,2,3]).has(2)).toBeTruthy()' },
      { description: 'has first element', assertion: 'expect(new Set([1,2,3]).has(1)).toBe(true)' },
      { description: 'has last element', assertion: 'expect(new Set([1,2,3]).has(3)).toBe(true)' },
    ],
    hints: ['has() returns true if the value exists in the Set'],
    tags: ['Set', 'has', 'method'],
  },
  {
    slug: 'set-has-2',
    title: 'Set has() — missing value',
    description: `## Set.prototype.has()\n\n\`has()\` returns false for a value not in the Set.\n\n**Challenge:** Verify that has() returns false for a missing value.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Set',
    method: 'has',
    initialCode: `// Check has() for missing value\nconst s = new Set([1, 2, 3])\n`,
    solution: `new Set([1,2,3]).has(99)`,
    tests: [
      { description: 'has(99) returns false', assertion: 'expect(new Set([1,2,3]).has(99)).toBe(false)' },
      { description: 'result is boolean', assertion: "expect(typeof new Set([1,2,3]).has(99)).toBe('boolean')" },
      { description: 'result is falsy', assertion: 'expect(new Set([1,2,3]).has(99)).toBeFalsy()' },
      { description: 'empty set has no values', assertion: 'expect(new Set().has(1)).toBe(false)' },
      { description: "has returns false not undefined", assertion: 'expect(new Set().has(1) === false).toBe(true)' },
    ],
    hints: ['has() returns false (not undefined) for missing values'],
    tags: ['Set', 'has', 'missing'],
  },
  {
    slug: 'set-has-3',
    title: 'Set has() — after add',
    description: `## Set.prototype.has()\n\nAfter add(), has() returns true for that value.\n\n**Challenge:** Verify that has() returns true after add().`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Set',
    method: 'has',
    initialCode: `// Add then check has()\nconst s = new Set()\ns.add('hello')\n`,
    solution: `const s = new Set(); s.add('hello'); s.has('hello')`,
    tests: [
      { description: "has after add is true", assertion: "const s = new Set(); s.add('x'); expect(s.has('x')).toBe(true)" },
      { description: "has false before add", assertion: "const s = new Set(); expect(s.has('x')).toBe(false)" },
      { description: "has true after add", assertion: "const s = new Set(); s.add(42); expect(s.has(42)).toBe(true)" },
      { description: "has works for multiple adds", assertion: "const s = new Set(); s.add(1); s.add(2); expect(s.has(1) && s.has(2)).toBe(true)" },
      { description: 'has falsy value after add', assertion: 'const s = new Set(); s.add(0); expect(s.has(0)).toBe(true)' },
    ],
    hints: ['has() checks for value existence, not value truthiness'],
    tags: ['Set', 'has', 'add'],
  },
  {
    slug: 'set-has-4',
    title: 'Set has() — after delete',
    description: `## Set.prototype.has()\n\nAfter delete(), has() returns false for that value.\n\n**Challenge:** Verify that has() returns false after delete().`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'Set',
    method: 'has',
    initialCode: `// Delete then check has()\nconst s = new Set([1, 2])\ns.delete(1)\n`,
    solution: `const s = new Set([1,2]); s.delete(1); s.has(1)`,
    tests: [
      { description: 'has(1) false after delete(1)', assertion: 'const s = new Set([1,2]); s.delete(1); expect(s.has(1)).toBe(false)' },
      { description: 'other values unaffected', assertion: 'const s = new Set([1,2]); s.delete(1); expect(s.has(2)).toBe(true)' },
      { description: 'has false is falsy', assertion: 'const s = new Set([1]); s.delete(1); expect(s.has(1)).toBeFalsy()' },
      { description: 'has false after clear', assertion: 'const s = new Set([1,2,3]); s.clear(); expect(s.has(1)).toBe(false)' },
      { description: 'can re-add after delete', assertion: 'const s = new Set([1]); s.delete(1); s.add(1); expect(s.has(1)).toBe(true)' },
    ],
    hints: ['delete() removes the value; has() returns false afterwards'],
    tags: ['Set', 'has', 'delete'],
  },
  {
    slug: 'set-has-5',
    title: 'Set has() — object reference',
    description: `## Set.prototype.has()\n\nhas() uses reference equality for objects.\n\n**Challenge:** Verify that has() uses reference equality for objects.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Set',
    method: 'has',
    initialCode: `// Object reference check\nconst obj = {}\nconst s = new Set([obj])\n`,
    solution: `const obj = {}; const s = new Set([obj]); s.has(obj)`,
    tests: [
      { description: 'has(sameRef) is true', assertion: 'const obj = {}; const s = new Set([obj]); expect(s.has(obj)).toBe(true)' },
      { description: 'has(diffRef) is false', assertion: 'const s = new Set([{}]); expect(s.has({})).toBe(false)' },
      { description: 'NaN is equal to NaN in Set', assertion: 'const s = new Set([NaN]); expect(s.has(NaN)).toBe(true)' },
      { description: '0 and -0 are same in Set', assertion: 'const s = new Set([0]); expect(s.has(-0)).toBe(true)' },
      { description: 'null works as value', assertion: 'const s = new Set([null]); expect(s.has(null)).toBe(true)' },
    ],
    hints: ['Sets use SameValueZero — NaN equals NaN, 0 equals -0, objects by reference'],
    tags: ['Set', 'has', 'reference'],
  },
]
