import type { Exercise } from '@/shared/types/exercises'

export const setSizeExercises: Exercise[] = [
  {
    slug: 'set-size-1',
    title: 'Set size — empty Set',
    description: `## Set.prototype.size\n\nThe \`size\` property returns the number of unique values. An empty Set has size 0.\n\n**Challenge:** Verify that \`new Set().size\` equals 0.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'Set',
    method: 'size',
    initialCode: `// Use new Set().size\n`,
    solution: `new Set().size`,
    tests: [
      { description: 'new Set().size equals 0', assertion: 'expect(new Set().size).toBe(0)' },
      { description: 'size is a number', assertion: "expect(typeof new Set().size).toBe('number')" },
      { description: 'size is falsy', assertion: 'expect(new Set().size).toBeFalsy()' },
      { description: 'size is non-negative', assertion: 'expect(new Set().size >= 0).toBe(true)' },
      { description: 'size is 0 strictly', assertion: 'expect(new Set().size === 0).toBe(true)' },
    ],
    hints: ['An empty Set has size 0'],
    tags: ['Set', 'size', 'property'],
  },
  {
    slug: 'set-size-2',
    title: 'Set size — after add',
    description: `## Set.prototype.size\n\nAdding a new value increases size by 1.\n\n**Challenge:** Verify that size equals 1 after one add().`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'Set',
    method: 'size',
    initialCode: `// Create a Set and call add()\nconst s = new Set()\ns.add(1)\n`,
    solution: `const s = new Set(); s.add(1); s.size`,
    tests: [
      { description: 'size is 1 after one add', assertion: 'const s = new Set(); s.add(1); expect(s.size).toBe(1)' },
      { description: 'size is 2 after two adds', assertion: 'const s = new Set(); s.add(1); s.add(2); expect(s.size).toBe(2)' },
      { description: 'size is a number', assertion: 'const s = new Set(); s.add(1); expect(typeof s.size).toBe(\'number\')' },
      { description: 'size is truthy after add', assertion: 'const s = new Set(); s.add(1); expect(s.size).toBeTruthy()' },
      { description: 'size correct for 3 adds', assertion: 'expect(new Set([1,2,3]).size).toBe(3)' },
    ],
    hints: ['Each unique value added increases size by 1'],
    tags: ['Set', 'size', 'add'],
  },
  {
    slug: 'set-size-3',
    title: 'Set size — deduplication',
    description: `## Set.prototype.size\n\nAdding the same value twice keeps size at 1.\n\n**Challenge:** Verify that adding a duplicate doesn't increase size.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'Set',
    method: 'size',
    initialCode: `// Add the same value twice\nconst s = new Set()\ns.add(1)\ns.add(1)\n`,
    solution: `const s = new Set(); s.add(1); s.add(1); s.size`,
    tests: [
      { description: 'size stays 1 after adding duplicate', assertion: 'const s = new Set(); s.add(1); s.add(1); expect(s.size).toBe(1)' },
      { description: 'size is 1 for Set([1,1,1])', assertion: 'expect(new Set([1,1,1]).size).toBe(1)' },
      { description: 'adding same value 3 times is size 1', assertion: 'const s = new Set(); s.add(5); s.add(5); s.add(5); expect(s.size).toBe(1)' },
      { description: 'string dedup', assertion: "expect(new Set(['a','a','b']).size).toBe(2)" },
      { description: 'mixed dedup', assertion: 'expect(new Set([1,1,2,2,3]).size).toBe(3)' },
    ],
    hints: ['Sets only store unique values; adding a duplicate is a no-op'],
    tags: ['Set', 'size', 'deduplication'],
  },
  {
    slug: 'set-size-4',
    title: 'Set size — after delete',
    description: `## Set.prototype.size\n\nDeleting a value decreases the size.\n\n**Challenge:** Verify that size decreases after delete().`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'Set',
    method: 'size',
    initialCode: `// Delete a value and check size\nconst s = new Set([1, 2, 3])\ns.delete(1)\n`,
    solution: `const s = new Set([1,2,3]); s.delete(1); s.size`,
    tests: [
      { description: 'size decreases after delete', assertion: 'const s = new Set([1,2,3]); s.delete(1); expect(s.size).toBe(2)' },
      { description: 'size is 0 after deleting only element', assertion: 'const s = new Set([1]); s.delete(1); expect(s.size).toBe(0)' },
      { description: 'delete non-existent does not change size', assertion: 'const s = new Set([1,2]); s.delete(99); expect(s.size).toBe(2)' },
      { description: 'size before delete is 3', assertion: 'expect(new Set([1,2,3]).size).toBe(3)' },
      { description: 'size after clear is 0', assertion: 'const s = new Set([1,2,3]); s.clear(); expect(s.size).toBe(0)' },
    ],
    hints: ['delete() removes the value and decreases size by 1'],
    tags: ['Set', 'size', 'delete'],
  },
  {
    slug: 'set-size-5',
    title: 'Set size — is a number',
    description: `## Set.prototype.size\n\nThe \`size\` property is always a non-negative integer.\n\n**Challenge:** Verify that \`typeof new Set().size\` is 'number'.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'Set',
    method: 'size',
    initialCode: `// Check typeof set.size\n`,
    solution: `typeof new Set().size`,
    tests: [
      { description: "typeof size is 'number'", assertion: "expect(typeof new Set().size).toBe('number')" },
      { description: 'size is not null', assertion: 'expect(new Set().size).not.toBeNull()' },
      { description: 'size is not undefined', assertion: 'expect(new Set().size).not.toBeUndefined()' },
      { description: 'size is non-negative', assertion: 'expect(new Set().size >= 0).toBe(true)' },
      { description: 'size is an integer', assertion: 'expect(Number.isInteger(new Set().size)).toBe(true)' },
    ],
    hints: ['size is always a non-negative integer'],
    tags: ['Set', 'size', 'typeof'],
  },
]
