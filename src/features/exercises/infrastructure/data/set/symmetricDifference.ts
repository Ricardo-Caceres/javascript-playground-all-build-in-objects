import type { Exercise } from '@/shared/types/exercises'

export const setSymmetricDifferenceExercises: Exercise[] = [
  {
    slug: 'set-symmetricdifference-1',
    title: 'Set symmetricDifference() — elements in either but not both',
    description: `## Set.prototype.symmetricDifference() (ES2025)\n\n\`setA.symmetricDifference(setB)\` returns elements in either Set but not in both.\n\n**Challenge:** Verify the symmetric difference.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Set',
    method: 'symmetricDifference',
    initialCode: `// Use symmetricDifference() - ES2025\nconst a = new Set([1,2,3])\nconst b = new Set([2,3,4])\n`,
    solution: `[...(new Set([1,2,3]) as any).symmetricDifference(new Set([2,3,4]))]`,
    tests: [
      { description: 'symmetric difference has [1,4]', assertion: 'expect(result.sort((a,b)=>a-b)).toEqual([1,4])' },
      { description: 'has 1 (only in A)', assertion: 'expect((new Set([1,2,3]) as any).symmetricDifference(new Set([2,3,4])).has(1)).toBe(true)' },
      { description: 'has 4 (only in B)', assertion: 'expect((new Set([1,2,3]) as any).symmetricDifference(new Set([2,3,4])).has(4)).toBe(true)' },
      { description: 'does not have 2 (in both)', assertion: 'expect((new Set([1,2,3]) as any).symmetricDifference(new Set([2,3,4])).has(2)).toBe(false)' },
      { description: 'result is a Set', assertion: 'expect((new Set([1,2,3]) as any).symmetricDifference(new Set([2,3,4])) instanceof Set).toBe(true)' },
    ],
    hints: ['symmetricDifference = (A union B) - (A intersection B)'],
    tags: ['Set', 'symmetricDifference', 'ES2025'],
  },
  {
    slug: 'set-symmetricdifference-2',
    title: 'Set symmetricDifference() — size',
    description: `## Set.prototype.symmetricDifference() (ES2025)\n\nVerify the size of the symmetric difference.\n\n**Challenge:** Verify size of symmetric difference.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Set',
    method: 'symmetricDifference',
    initialCode: `// Check symmetric difference size\nconst a = new Set([1,2,3])\nconst b = new Set([2,3,4])\n`,
    solution: `(new Set([1,2,3]) as any).symmetricDifference(new Set([2,3,4])).size`,
    tests: [
      { description: 'size is 2', assertion: 'expect(result).toBe(2)' },
      { description: 'disjoint: sym diff size is sum of both', assertion: 'expect(result).toBe(4)' },
      { description: 'identical: sym diff size is 0', assertion: 'expect(result).toBe(0)' },
      { description: 'size is a number', assertion: "expect(typeof (new Set([1]) as any).symmetricDifference(new Set([2])).size).toBe('number')" },
      { description: 'sym diff with empty is same size', assertion: 'expect((new Set([1,2,3]) as any).symmetricDifference(new Set()).size).toBe(3)' },
    ],
    hints: ['symmetric difference size = |A| + |B| - 2*|A∩B|'],
    tags: ['Set', 'symmetricDifference', 'size'],
  },
  {
    slug: 'set-symmetricdifference-3',
    title: 'Set symmetricDifference() — identical sets → empty',
    description: `## Set.prototype.symmetricDifference() (ES2025)\n\nIdentical Sets have an empty symmetric difference.\n\n**Challenge:** Verify that symmetric difference of identical Sets is empty.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Set',
    method: 'symmetricDifference',
    initialCode: `// Identical sets\nconst a = new Set([1,2,3])\nconst b = new Set([1,2,3])\n`,
    solution: `(new Set([1,2,3]) as any).symmetricDifference(new Set([1,2,3])).size`,
    tests: [
      { description: 'identical sets: sym diff size is 0', assertion: 'expect(result).toBe(0)' },
      { description: 'result is empty Set', assertion: 'expect([...(new Set([1,2]) as any).symmetricDifference(new Set([1,2]))]).toEqual([])' },
      { description: 'result is a Set', assertion: 'expect((new Set([1]) as any).symmetricDifference(new Set([1])) instanceof Set).toBe(true)' },
      { description: 'empty with empty: sym diff is empty', assertion: 'expect((new Set() as any).symmetricDifference(new Set()).size).toBe(0)' },
      { description: 'size 0 is falsy', assertion: 'expect((new Set([1,2]) as any).symmetricDifference(new Set([1,2])).size).toBeFalsy()' },
    ],
    hints: ['When A equals B, symmetric difference is empty — all elements are in both'],
    tags: ['Set', 'symmetricDifference', 'identical'],
  },
  {
    slug: 'set-symmetricdifference-4',
    title: 'Set symmetricDifference() — disjoint = union',
    description: `## Set.prototype.symmetricDifference() (ES2025)\n\nFor disjoint Sets, symmetric difference equals their union.\n\n**Challenge:** Verify that disjoint symmetric difference equals union.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Set',
    method: 'symmetricDifference',
    initialCode: `// Disjoint sets\nconst a = new Set([1,2])\nconst b = new Set([3,4])\n`,
    solution: `(new Set([1,2]) as any).symmetricDifference(new Set([3,4])).size`,
    tests: [
      { description: 'disjoint: sym diff size is 4', assertion: 'expect(result).toBe(4)' },
      { description: 'disjoint: all elements present', assertion: 'const r = (new Set([1,2]) as any).symmetricDifference(new Set([3,4])); expect(r.has(1) && r.has(2) && r.has(3) && r.has(4)).toBe(true)' },
      { description: 'sym diff equals union for disjoint', assertion: 'const a = new Set([1,2]); const b = new Set([3,4]); expect((a as any).symmetricDifference(b).size).toBe((a as any).union(b).size)' },
      { description: 'result is a Set', assertion: 'expect((new Set([1,2]) as any).symmetricDifference(new Set([3,4])) instanceof Set).toBe(true)' },
      { description: 'includes all disjoint elements', assertion: 'expect((new Set([1,2]) as any).symmetricDifference(new Set([3,4])).has(3)).toBe(true)' },
    ],
    hints: ['For disjoint Sets, every element is exclusive to one Set, so sym diff = union'],
    tags: ['Set', 'symmetricDifference', 'disjoint'],
  },
  {
    slug: 'set-symmetricdifference-5',
    title: 'Set symmetricDifference() — originals unchanged',
    description: `## Set.prototype.symmetricDifference() (ES2025)\n\nsymmetricDifference() does not modify the original Sets.\n\n**Challenge:** Verify originals are unchanged.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'Set',
    method: 'symmetricDifference',
    initialCode: `// Check originals\nconst a = new Set([1,2,3])\nconst b = new Set([2,3,4])\n`,
    solution: `const a = new Set([1,2,3]); const b = new Set([2,3,4]); (a as any).symmetricDifference(b); a.size`,
    tests: [
      { description: 'a unchanged after sym diff', assertion: 'const a = new Set([1,2,3]); (a as any).symmetricDifference(new Set([2,3])); expect(a.size).toBe(3)' },
      { description: 'b unchanged after sym diff', assertion: 'const a = new Set([1,2,3]); const b = new Set([2,3,4]); (a as any).symmetricDifference(b); expect(b.size).toBe(3)' },
      { description: 'result is a new Set', assertion: 'const a = new Set([1,2]); const r = (a as any).symmetricDifference(new Set([3])); expect(r).not.toBe(a)' },
      { description: 'a still has element 1', assertion: 'const a = new Set([1,2,3]); (a as any).symmetricDifference(new Set([1])); expect(a.has(1)).toBe(true)' },
      { description: 'sym diff returns new Set', assertion: 'expect((new Set([1,2]) as any).symmetricDifference(new Set([3,4])) instanceof Set).toBe(true)' },
    ],
    hints: ['symmetricDifference() returns a new Set; originals unchanged'],
    tags: ['Set', 'symmetricDifference', 'immutable'],
  },
]
