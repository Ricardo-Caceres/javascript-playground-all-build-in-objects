import type { Exercise } from '@/shared/types/exercises'

export const symbolToPrimitiveExercises: Exercise[] = [
  {
    slug: 'symbol-toprimitive-1',
    title: 'Symbol.toPrimitive — typeof is symbol',
    description: `## Symbol.toPrimitive\n\n\`Symbol.toPrimitive\` is a well-known symbol used to define custom type coercion for objects.\n\n**Challenge:** Verify its type.`,
    category: 'static-property',
    difficulty: 'beginner',
    builtIn: 'Symbol',
    method: 'toPrimitive',
    initialCode: `// Well-known symbol\n`,
    solution: `typeof Symbol.toPrimitive`,
    tests: [
      { description: "typeof Symbol.toPrimitive is 'symbol'", assertion: "expect(typeof Symbol.toPrimitive).toBe('symbol')" },
      { description: 'Symbol.toPrimitive is truthy', assertion: 'expect(Symbol.toPrimitive).toBeTruthy()' },
      { description: 'is a property of Symbol', assertion: "expect('toPrimitive' in Symbol).toBe(true)" },
      { description: 'same reference', assertion: 'expect(Symbol.toPrimitive === Symbol.toPrimitive).toBe(true)' },
      { description: 'not a string', assertion: "expect(typeof Symbol.toPrimitive === 'string').toBe(false)" },
    ],
    hints: ['Symbol.toPrimitive is a well-known symbol on the Symbol constructor.'],
    tags: ['symbol', 'Symbol.toPrimitive', 'well-known'],
  },
  {
    slug: 'symbol-toprimitive-2',
    title: 'Symbol.toPrimitive — number hint',
    description: `## Custom Coercion with Number Hint\n\nWhen an object defines \`[Symbol.toPrimitive]\`, it receives a \`hint\` of \`'number'\`, \`'string'\`, or \`'default'\`.\n\n**Challenge:** Verify that \`+obj\` triggers the number hint.`,
    category: 'static-property',
    difficulty: 'intermediate',
    builtIn: 'Symbol',
    method: 'toPrimitive',
    initialCode: `const obj = {\n  [Symbol.toPrimitive](hint: string) {\n    return hint === 'number' ? 42 : 'hello'\n  }\n}\n`,
    solution: `+{ [Symbol.toPrimitive](hint: string) { return hint === 'number' ? 42 : 'hello' } }`,
    tests: [
      { description: '+obj returns 42 (number hint)', assertion: "const obj = { [Symbol.toPrimitive](hint: string) { return hint === 'number' ? 42 : 'hello' } }; expect(+obj).toBe(42)" },
      { description: 'unary + triggers number hint', assertion: "const o = { [Symbol.toPrimitive](h: string) { return h === 'number' ? 99 : 0 } }; expect(+o).toBe(99)" },
      { description: 'result is a number', assertion: "const o = { [Symbol.toPrimitive](h: string) { return h === 'number' ? 7 : 0 } }; expect(typeof +o).toBe('number')" },
      { description: 'Symbol.toPrimitive is callable', assertion: "expect(typeof Symbol.toPrimitive).toBe('symbol')" },
      { description: 'works in arithmetic', assertion: "const o = { [Symbol.toPrimitive](h: string) { return h === 'number' ? 10 : 0 } }; expect(+o + 5).toBe(15)" },
    ],
    hints: ['Use the unary + operator to trigger the number hint.'],
    tags: ['symbol', 'Symbol.toPrimitive', 'coercion', 'hint'],
  },
  {
    slug: 'symbol-toprimitive-3',
    title: 'Symbol.toPrimitive — string hint',
    description: `## Custom Coercion with String Hint\n\nTemplate literals trigger the \`'string'\` hint when an object has \`[Symbol.toPrimitive]\`.\n\n**Challenge:** Verify that template literal coercion triggers the string hint.`,
    category: 'static-property',
    difficulty: 'intermediate',
    builtIn: 'Symbol',
    method: 'toPrimitive',
    initialCode: `const obj = {\n  [Symbol.toPrimitive](hint: string) {\n    return hint === 'string' ? 'world' : 0\n  }\n}\n`,
    solution: '`${({ [Symbol.toPrimitive](hint: string) { return hint === \'string\' ? \'world\' : 0 } })}`',
    tests: [
      { description: 'template literal triggers string hint', assertion: "const obj = { [Symbol.toPrimitive](hint: string) { return hint === 'string' ? 'world' : 0 } }; expect(`${obj}`).toBe('world')" },
      { description: 'string hint returns the string value', assertion: "const o = { [Symbol.toPrimitive](h: string) { return h === 'string' ? 'hi' : 0 } }; expect(`${o}`).toBe('hi')" },
      { description: 'result is a string', assertion: "const o = { [Symbol.toPrimitive](h: string) { return h === 'string' ? 'ok' : 0 } }; expect(typeof `${o}`).toBe('string')" },
      { description: 'Symbol.toPrimitive is a symbol', assertion: "expect(typeof Symbol.toPrimitive).toBe('symbol')" },
      { description: 'works in concatenation', assertion: "const o = { [Symbol.toPrimitive](h: string) { return h === 'string' ? 'x' : 0 } }; expect(`${o}y`).toBe('xy')" },
    ],
    hints: ['Template literals use the string hint.'],
    tags: ['symbol', 'Symbol.toPrimitive', 'coercion', 'string-hint'],
  },
  {
    slug: 'symbol-toprimitive-4',
    title: 'Symbol.toPrimitive — typeof is always symbol',
    description: `## Symbol.toPrimitive Type\n\nRegardless of what hint an object uses, \`Symbol.toPrimitive\` itself is always a symbol.\n\n**Challenge:** Confirm the type once more.`,
    category: 'static-property',
    difficulty: 'beginner',
    builtIn: 'Symbol',
    method: 'toPrimitive',
    initialCode: ``,
    solution: `typeof Symbol.toPrimitive`,
    tests: [
      { description: "typeof is 'symbol'", assertion: "expect(typeof Symbol.toPrimitive).toBe('symbol')" },
      { description: 'not undefined', assertion: 'expect(Symbol.toPrimitive).not.toBeUndefined()' },
      { description: 'not null', assertion: 'expect(Symbol.toPrimitive).not.toBeNull()' },
      { description: 'is truthy', assertion: 'expect(Symbol.toPrimitive).toBeTruthy()' },
      { description: 'same reference always', assertion: 'expect(Symbol.toPrimitive === Symbol.toPrimitive).toBe(true)' },
    ],
    hints: ['Well-known symbols always have type "symbol".'],
    tags: ['symbol', 'Symbol.toPrimitive', 'typeof'],
  },
  {
    slug: 'symbol-toprimitive-5',
    title: 'Symbol.toPrimitive — not in global registry',
    description: `## Symbol.toPrimitive is Well-Known, Not Global\n\n\`Symbol.toPrimitive !== Symbol('Symbol.toPrimitive')\` — the well-known symbol is distinct from any locally created symbol with the same description.\n\n**Challenge:** Verify they are not equal.`,
    category: 'static-property',
    difficulty: 'intermediate',
    builtIn: 'Symbol',
    method: 'toPrimitive',
    initialCode: ``,
    solution: `Symbol.toPrimitive !== Symbol('Symbol.toPrimitive')`,
    tests: [
      { description: "toPrimitive !== Symbol('Symbol.toPrimitive')", assertion: "expect(Symbol.toPrimitive !== Symbol('Symbol.toPrimitive')).toBe(true)" },
      { description: 'not in global registry', assertion: 'expect(Symbol.keyFor(Symbol.toPrimitive)).toBeUndefined()' },
      { description: 'well-known symbol identity', assertion: 'expect(Symbol.toPrimitive === Symbol.toPrimitive).toBe(true)' },
      { description: "local symbol is not equal", assertion: "expect(Symbol.toPrimitive === Symbol('Symbol.toPrimitive')).toBe(false)" },
      { description: 'both are symbols', assertion: "expect(typeof Symbol.toPrimitive).toBe('symbol')" },
    ],
    hints: ['Well-known symbols are not global registry symbols.'],
    tags: ['symbol', 'Symbol.toPrimitive', 'well-known', 'uniqueness'],
  },
]
