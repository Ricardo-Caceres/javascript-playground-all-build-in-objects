import type { Exercise } from '@/shared/types/exercises'

export const textDecoderConstructorExercises: Exercise[] = [
  {
    slug: 'textdecoder-constructor-1',
    title: 'TextDecoder — default encoding',
    description: `## TextDecoder Constructor\n\n\`new TextDecoder()\` defaults to UTF-8. The \`encoding\` property returns \`'utf-8'\`.\n\n**Challenge:** Access the \`encoding\` property of a default TextDecoder.`,
    category: 'constructor',
    difficulty: 'beginner',
    builtIn: 'TextDecoder',
    initialCode: `// Check the default encoding\nconst dec = new TextDecoder()\n`,
    solution: `new TextDecoder().encoding`,
    tests: [
      { description: "default encoding is 'utf-8'", assertion: "expect(result).toBe('utf-8')" },
      { description: 'encoding is a string', assertion: "expect(typeof result).toBe('string')" },
      { description: 'encoding is not empty', assertion: "expect(result.length).toBeGreaterThan(0)" },
      { description: 'two decoders share the same default encoding', assertion: "expect(result).toBe(result)" },
      { description: "encoding is not 'utf-16'", assertion: "expect(result).not.toBe('utf-16')" },
    ],
    hints: ['TextDecoder defaults to UTF-8 when no label is provided.'],
    tags: ['TextDecoder', 'constructor', 'encoding'],
  },
  {
    slug: 'textdecoder-constructor-2',
    title: 'TextDecoder — explicit utf-8 label',
    description: `## TextDecoder with Explicit Label\n\n\`new TextDecoder('utf-8').encoding\` returns \`'utf-8'\`.\n\n**Challenge:** Create a TextDecoder with \`'utf-8'\` and verify the encoding property.`,
    category: 'constructor',
    difficulty: 'beginner',
    builtIn: 'TextDecoder',
    initialCode: `// Create with explicit 'utf-8' label\nconst dec = new TextDecoder('utf-8')\n`,
    solution: `new TextDecoder('utf-8').encoding`,
    tests: [
      { description: "explicit 'utf-8' encoding property", assertion: "expect(result).toBe('utf-8')" },
      { description: "explicit vs default are equal", assertion: "expect(result).toBe(result)" },
      { description: 'is a string', assertion: "expect(typeof result).toBe('string')" },
      { description: 'instanceof TextDecoder', assertion: "expect(new TextDecoder('utf-8') instanceof TextDecoder).toBe(true)" },
      { description: 'not null', assertion: "expect(new TextDecoder('utf-8')).not.toBeNull()" },
    ],
    hints: ['Passing UTF-8 explicitly is the same as the default.'],
    tags: ['TextDecoder', 'constructor', 'encoding', 'utf-8'],
  },
  {
    slug: 'textdecoder-constructor-3',
    title: 'TextDecoder — fatal property',
    description: `## TextDecoder fatal Property\n\nThe \`fatal\` option controls error handling. By default it is \`false\`, meaning invalid bytes are replaced with the replacement character U+FFFD.\n\n**Challenge:** Verify that \`new TextDecoder().fatal === false\`.`,
    category: 'constructor',
    difficulty: 'beginner',
    builtIn: 'TextDecoder',
    initialCode: `// Check the fatal property\nconst dec = new TextDecoder()\n`,
    solution: `new TextDecoder().fatal`,
    tests: [
      { description: 'fatal defaults to false', assertion: 'expect(result).toBe(false)' },
      { description: 'fatal is a boolean', assertion: "expect(typeof result).toBe('boolean')" },
      { description: 'fatal with {fatal:false} is false', assertion: "expect(new TextDecoder('utf-8', { fatal: false }).fatal).toBe(false)" },
      { description: 'fatal with {fatal:true} is true', assertion: "expect(new TextDecoder('utf-8', { fatal: true }).fatal).toBe(true)" },
      { description: 'not undefined', assertion: 'expect(result).not.toBeUndefined()' },
    ],
    hints: ['fatal: false means invalid bytes are silently replaced.'],
    tags: ['TextDecoder', 'constructor', 'fatal'],
  },
  {
    slug: 'textdecoder-constructor-4',
    title: 'TextDecoder — ignoreBOM property',
    description: `## TextDecoder ignoreBOM Property\n\nWhen \`ignoreBOM\` is \`false\` (the default), a leading byte-order mark is stripped from the decoded output. The property reflects the constructor option.\n\n**Challenge:** Verify that \`new TextDecoder().ignoreBOM === false\`.`,
    category: 'constructor',
    difficulty: 'intermediate',
    builtIn: 'TextDecoder',
    initialCode: `// Check the ignoreBOM property\nconst dec = new TextDecoder()\n`,
    solution: `new TextDecoder().ignoreBOM`,
    tests: [
      { description: 'ignoreBOM defaults to false', assertion: 'expect(result).toBe(false)' },
      { description: 'ignoreBOM is a boolean', assertion: "expect(typeof result).toBe('boolean')" },
      { description: 'ignoreBOM with {ignoreBOM:true} is true', assertion: "expect(new TextDecoder('utf-8', { ignoreBOM: true }).ignoreBOM).toBe(true)" },
      { description: 'ignoreBOM with {ignoreBOM:false} is false', assertion: "expect(new TextDecoder('utf-8', { ignoreBOM: false }).ignoreBOM).toBe(false)" },
      { description: 'not undefined', assertion: 'expect(result).not.toBeUndefined()' },
    ],
    hints: ['ignoreBOM defaults to false; set it to true to keep the BOM in output.'],
    tags: ['TextDecoder', 'constructor', 'ignoreBOM'],
  },
  {
    slug: 'textdecoder-constructor-5',
    title: 'TextDecoder — instanceof check',
    description: `## TextDecoder instanceof\n\n\`new TextDecoder() instanceof TextDecoder\` is \`true\`.\n\n**Challenge:** Verify the instanceof check.`,
    category: 'constructor',
    difficulty: 'beginner',
    builtIn: 'TextDecoder',
    initialCode: `// Check instanceof\nconst dec = new TextDecoder()\n`,
    solution: `new TextDecoder() instanceof TextDecoder`,
    tests: [
      { description: 'instanceof TextDecoder', assertion: 'expect(result).toBe(true)' },
      { description: 'instanceof Object', assertion: 'expect(new TextDecoder() instanceof Object).toBe(true)' },
      { description: 'two instances are different', assertion: 'expect(new TextDecoder() === new TextDecoder()).toBe(false)' },
      { description: 'is truthy', assertion: 'expect(new TextDecoder()).toBeTruthy()' },
      { description: 'not null', assertion: 'expect(new TextDecoder()).not.toBeNull()' },
    ],
    hints: ['TextDecoder is a constructor, so new TextDecoder() is an instance of it.'],
    tags: ['TextDecoder', 'constructor', 'instanceof'],
  },
]
