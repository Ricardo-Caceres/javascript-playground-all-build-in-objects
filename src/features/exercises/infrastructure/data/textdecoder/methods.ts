import type { Exercise } from '@/shared/types/exercises'

export const textDecoderMethodsExercises: Exercise[] = [
  {
    slug: 'textdecoder-methods-1',
    title: "TextDecoder.decode — round-trip 'hello'",
    description: `## decode() Round-trip\n\nEncoding a string with \`TextEncoder\` and then decoding with \`TextDecoder\` should return the original string.\n\n**Challenge:** Verify that \`new TextDecoder().decode(new TextEncoder().encode('hello')) === 'hello'\`.`,
    category: 'instance-method',
    difficulty: 'intermediate',
    builtIn: 'TextDecoder',
    initialCode: `// Encode then decode\nconst enc = new TextEncoder()\nconst dec = new TextDecoder()\ndec.decode(enc.encode('hello'))\n`,
    solution: `new TextDecoder().decode(new TextEncoder().encode('hello'))`,
    tests: [
      { description: "round-trip 'hello'", assertion: "expect(result).toBe('hello')" },
      { description: "round-trip 'world'", assertion: "expect(new TextDecoder().decode(new TextEncoder().encode('world'))).toBe('world')" },
      { description: 'result is a string', assertion: "expect(typeof new TextDecoder().decode(new TextEncoder().encode('hi'))).toBe('string')" },
      { description: 'result has correct length', assertion: "expect(result.length).toBe(5)" },
      { description: "round-trip 'abc'", assertion: "expect(new TextDecoder().decode(new TextEncoder().encode('abc'))).toBe('abc')" },
    ],
    hints: ['TextEncoder and TextDecoder are inverse operations for valid UTF-8.'],
    tags: ['TextDecoder', 'decode', 'round-trip'],
  },
  {
    slug: 'textdecoder-methods-2',
    title: 'TextDecoder.decode — empty Uint8Array',
    description: `## Decoding an Empty Buffer\n\n\`new TextDecoder().decode(new Uint8Array([]))\` returns an empty string \`''\`.\n\n**Challenge:** Verify that decoding an empty byte array yields an empty string.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'TextDecoder',
    initialCode: `// Decode an empty Uint8Array\nconst dec = new TextDecoder()\ndec.decode(new Uint8Array([]))\n`,
    solution: `new TextDecoder().decode(new Uint8Array([]))`,
    tests: [
      { description: "decode empty array is ''", assertion: "expect(result).toBe('')" },
      { description: 'result is a string', assertion: "expect(typeof result).toBe('string')" },
      { description: 'result length is 0', assertion: "expect(result.length).toBe(0)" },
      { description: 'empty string is falsy', assertion: "expect(result).toBeFalsy()" },
      { description: 'result equals empty string literal', assertion: "expect(result).toStrictEqual('')" },
    ],
    hints: ['No bytes means no characters — the result is an empty string.'],
    tags: ['TextDecoder', 'decode', 'empty'],
  },
  {
    slug: 'textdecoder-methods-3',
    title: "TextDecoder.decode — single byte 72 is 'H'",
    description: `## Decoding a Single Byte\n\nThe byte value 72 is the ASCII code for \`'H'\`. Decoding \`new Uint8Array([72])\` yields \`'H'\`.\n\n**Challenge:** Verify that \`decode(new Uint8Array([72])) === 'H'\`.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'TextDecoder',
    initialCode: `// Decode a single byte\nconst dec = new TextDecoder()\ndec.decode(new Uint8Array([72]))\n`,
    solution: `new TextDecoder().decode(new Uint8Array([72]))`,
    tests: [
      { description: "decode([72]) is 'H'", assertion: "expect(result).toBe('H')" },
      { description: "decode([65]) is 'A'", assertion: "expect(new TextDecoder().decode(new Uint8Array([65]))).toBe('A')" },
      { description: "decode([90]) is 'Z'", assertion: "expect(new TextDecoder().decode(new Uint8Array([90]))).toBe('Z')" },
      { description: 'result is a string', assertion: "expect(typeof result).toBe('string')" },
      { description: 'result length is 1', assertion: "expect(result.length).toBe(1)" },
    ],
    hints: ['ASCII byte 72 corresponds to the character H.'],
    tags: ['TextDecoder', 'decode', 'ASCII'],
  },
  {
    slug: 'textdecoder-methods-4',
    title: "TextDecoder.decode — round-trip '€'",
    description: `## Decoding Multi-byte UTF-8\n\nThe Euro sign \`'€'\` encodes as 3 bytes in UTF-8. Decoding those bytes recovers the original character.\n\n**Challenge:** Verify the round-trip for \`'€'\`.`,
    category: 'instance-method',
    difficulty: 'advanced',
    builtIn: 'TextDecoder',
    initialCode: `// Encode and decode the Euro sign\nconst enc = new TextEncoder()\nconst dec = new TextDecoder()\ndec.decode(enc.encode('€'))\n`,
    solution: `new TextDecoder().decode(new TextEncoder().encode('€'))`,
    tests: [
      { description: "round-trip '€'", assertion: "expect(result).toBe('€')" },
      { description: 'result is a string', assertion: "expect(typeof result).toBe('string')" },
      { description: 'result length is 1 (single character)', assertion: "expect(result.length).toBe(1)" },
      { description: 'encoded bytes length is 3', assertion: "expect(new TextEncoder().encode('€').length).toBe(3)" },
      { description: "decoded matches '\\u20AC'", assertion: "expect(result).toBe('\\u20AC')" },
    ],
    hints: ['€ is U+20AC. Despite 3 bytes, decode returns a single character.'],
    tags: ['TextDecoder', 'decode', 'multi-byte', 'UTF-8'],
  },
  {
    slug: 'textdecoder-methods-5',
    title: "TextDecoder.decode — decoded length of 'abc'",
    description: `## Decoded String Length\n\nDecoding the encoded bytes of \`'abc'\` should give a string of length 3.\n\n**Challenge:** Verify that \`decode(encode('abc')).length === 3\`.`,
    category: 'instance-method',
    difficulty: 'beginner',
    builtIn: 'TextDecoder',
    initialCode: `// Verify decoded string length\nconst enc = new TextEncoder()\nconst dec = new TextDecoder()\ndec.decode(enc.encode('abc')).length\n`,
    solution: `new TextDecoder().decode(new TextEncoder().encode('abc')).length`,
    tests: [
      { description: "decode(encode('abc')).length is 3", assertion: "expect(result).toBe(3)" },
      { description: "decode(encode('hello')).length is 5", assertion: "expect(result.length).toBe(5)" },
      { description: "decode(encode('')).length is 0", assertion: "expect(new TextDecoder().decode(new TextEncoder().encode('')).length).toBe(0)" },
      { description: 'result is a string', assertion: "expect(typeof new TextDecoder().decode(new TextEncoder().encode('abc'))).toBe('string')" },
      { description: "result equals 'abc'", assertion: "expect(new TextDecoder().decode(new TextEncoder().encode('abc'))).toBe('abc')" },
    ],
    hints: ['For ASCII strings, the decoded length equals the original string length.'],
    tags: ['TextDecoder', 'decode', 'length'],
  },
]
