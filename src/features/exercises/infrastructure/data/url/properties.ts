import type { Exercise } from '@/shared/types/exercises'

export const urlPropertiesExercises: Exercise[] = [
  {
    slug: 'url-instance-property-1',
    title: 'URL Properties — href and protocol',
    description: `## URL Instance Properties — href & protocol\n\n\`href\` returns the full serialized URL. \`protocol\` returns the scheme followed by \`:\`.\n\n**Challenge:** Parse \`'https://example.com/path'\` and inspect \`href\` and \`protocol\`.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'URL',
    initialCode: `const u = new URL('https://example.com/path')\n// inspect u.href and u.protocol\n`,
    solution: `new URL('https://example.com/path').protocol`,
    tests: [
      { description: 'protocol is https:', assertion: "expect(new URL('https://example.com/path').protocol).toBe('https:')" },
      { description: 'http protocol is http:', assertion: "expect(new URL('http://example.com').protocol).toBe('http:')" },
      { description: 'href contains the full URL', assertion: "expect(new URL('https://example.com/path').href).toContain('https://example.com/path')" },
      { description: 'href is a string', assertion: "expect(typeof new URL('https://example.com').href).toBe('string')" },
      { description: 'protocol is a string', assertion: "expect(typeof new URL('https://example.com').protocol).toBe('string')" },
    ],
    hints: ['protocol always ends with a colon'],
    tags: ['URL', 'instance-property', 'href', 'protocol'],
  },
  {
    slug: 'url-instance-property-2',
    title: 'URL Properties — hostname, port, host',
    description: `## URL Instance Properties — hostname, port, host\n\n\`hostname\` is the domain without port. \`port\` is the port string (empty if default). \`host\` is hostname + port.\n\n**Challenge:** Parse \`'http://host.com:8080/p'\` and check these properties.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'URL',
    initialCode: `const u = new URL('http://host.com:8080/p')\n// inspect u.hostname, u.port, u.host\n`,
    solution: `new URL('http://host.com:8080/p').port`,
    tests: [
      { description: "hostname is 'host.com'", assertion: "expect(new URL('http://host.com:8080/p').hostname).toBe('host.com')" },
      { description: "port is '8080'", assertion: "expect(new URL('http://host.com:8080/p').port).toBe('8080')" },
      { description: "host includes port", assertion: "expect(new URL('http://host.com:8080/p').host).toBe('host.com:8080')" },
      { description: 'default port is empty string', assertion: "expect(new URL('https://example.com').port).toBe('')" },
      { description: 'host without port equals hostname', assertion: "expect(new URL('https://example.com').host).toBe('example.com')" },
    ],
    hints: ['port is empty string when using the default port for the protocol'],
    tags: ['URL', 'instance-property', 'hostname', 'port', 'host'],
  },
  {
    slug: 'url-instance-property-3',
    title: 'URL Properties — pathname and search',
    description: `## URL Instance Properties — pathname & search\n\n\`pathname\` is the path portion. \`search\` is the query string including \`?\`.\n\n**Challenge:** Parse \`'https://a.com/path/to/page?q=hello&lang=en'\` and inspect \`pathname\` and \`search\`.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'URL',
    initialCode: `const u = new URL('https://a.com/path/to/page?q=hello&lang=en')\n// inspect u.pathname and u.search\n`,
    solution: `new URL('https://a.com/path/to/page?q=hello&lang=en').search`,
    tests: [
      { description: "pathname is '/path/to/page'", assertion: "expect(new URL('https://a.com/path/to/page?q=hello').pathname).toBe('/path/to/page')" },
      { description: "search starts with ?", assertion: "expect(new URL('https://a.com?q=1').search).toBe('?q=1')" },
      { description: 'no query → search is empty string', assertion: "expect(new URL('https://a.com/path').search).toBe('')" },
      { description: 'no path → pathname is /', assertion: "expect(new URL('https://a.com').pathname).toBe('/')" },
      { description: 'search includes both params', assertion: "expect(new URL('https://a.com?q=hello&lang=en').search).toContain('lang=en')" },
    ],
    hints: ['search includes the leading ? character'],
    tags: ['URL', 'instance-property', 'pathname', 'search'],
  },
  {
    slug: 'url-instance-property-4',
    title: 'URL Properties — hash and origin',
    description: `## URL Instance Properties — hash & origin\n\n\`hash\` is the fragment including \`#\`. \`origin\` is protocol + hostname + port.\n\n**Challenge:** Check \`hash\` and \`origin\` on various URLs.`,
    category: 'instance-property',
    difficulty: 'beginner',
    builtIn: 'URL',
    initialCode: `const u = new URL('https://example.com:8080/page#section')\n// inspect u.hash and u.origin\n`,
    solution: `new URL('https://example.com:8080/page#section').origin`,
    tests: [
      { description: "hash is '#section'", assertion: "expect(new URL('https://a.com#section').hash).toBe('#section')" },
      { description: 'no hash → hash is empty string', assertion: "expect(new URL('https://a.com').hash).toBe('')" },
      { description: "origin is protocol+host", assertion: "expect(new URL('https://example.com:8080/p').origin).toBe('https://example.com:8080')" },
      { description: 'origin without port', assertion: "expect(new URL('https://example.com/p').origin).toBe('https://example.com')" },
      { description: 'origin does not include path', assertion: "expect(new URL('https://example.com/path').origin).not.toContain('/path')" },
    ],
    hints: ['hash includes the # character; origin never includes path or query'],
    tags: ['URL', 'instance-property', 'hash', 'origin'],
  },
  {
    slug: 'url-instance-property-5',
    title: 'URL Properties — searchParams live round-trip',
    description: `## URL Instance Properties — searchParams (live)\n\n\`url.searchParams\` is a live \`URLSearchParams\` object. Mutating it propagates back to \`url.href\` (and \`url.search\`).\n\n**Challenge:** Mutate \`url.searchParams\` and verify \`url.href\` reflects the change.`,
    category: 'instance-property',
    difficulty: 'advanced',
    builtIn: 'URL',
    initialCode: `const url = new URL('https://example.com?a=1')\nurl.searchParams.set('a', '99')\n// url.href should now reflect a=99\n`,
    solution: `const url = new URL('https://example.com?a=1'); url.searchParams.set('a', '99'); url.href`,
    tests: [
      { description: 'mutating searchParams updates href', assertion: "const u=new URL('https://example.com?a=1'); u.searchParams.set('a','99'); expect(u.href).toContain('a=99')" },
      { description: 'old value removed from href', assertion: "const u2=new URL('https://example.com?a=1'); u2.searchParams.set('a','99'); expect(u2.href).not.toContain('a=1')" },
      { description: 'searchParams.append updates href', assertion: "const u3=new URL('https://example.com?a=1'); u3.searchParams.append('b','2'); expect(u3.href).toContain('b=2')" },
      { description: 'searchParams.delete updates href', assertion: "const u4=new URL('https://example.com?a=1&b=2'); u4.searchParams.delete('a'); expect(u4.href).not.toContain('a=1')" },
      { description: 'searchParams is live URLSearchParams', assertion: "expect(new URL('https://example.com?x=1').searchParams instanceof URLSearchParams).toBe(true)" },
    ],
    hints: ['url.searchParams is a live view — changes to it are reflected in url.href immediately'],
    tags: ['URL', 'instance-property', 'searchParams', 'mutation', 'live'],
  },
]
