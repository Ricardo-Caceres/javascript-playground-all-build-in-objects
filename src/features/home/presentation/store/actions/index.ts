// Home action creators — plain (non-async) actions not tied to a slice
// Useful for cross-slice actions or actions dispatched from multiple features
// import { createAction } from '@reduxjs/toolkit'
//
// export const resetHomeFeature = createAction('home/reset')
